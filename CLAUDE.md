# secure-test — Claude project notes

PSD macOS secure-testing browser + teacher-facing assessment-authoring "design-tool" that feeds it. Bun workspace monorepo.

## Layout
- `design-tool/` — Next.js 16 web app (teacher-facing authoring + the student plane API). Drizzle + Postgres, Zod, KaTeX, Google OIDC+PKCE (ADR 0017).
- `client/` — the macOS student client (Phase 5). `SecureTestCore/` is a SwiftPM package holding all the logic so it is testable headlessly; `SecureTest.xcodeproj` is the AppKit app. Supersedes `poc-b-test-loop/client`. See `client/README.md`.
- `packages/` — shared code (Zod schemas, etc.).
- `poc-*/` — proof-of-concept clients (AAC capture, test loop, ClassLink SSO — the last one historical since ADR 0017). Kept as the empirical record; PoC-A is still the spike for the ScreenCaptureKit-inside-AAC question.

## Dev (run the app from `design-tool/`, not repo root)
- `bun install` at root (workspace).
- `cd design-tool && bun run dev` — the dev server runs from `design-tool/`. Running it from the repo root is a recurring mistake.
- Copy `design-tool/.env.local.example` → `.env.local` and fill it before `dev`; unset OIDC/env vars are a common startup failure.
- Next 16 uses `proxy.ts` (renamed from `middleware`); a missing export there breaks dev.
- DB: `bun run db:generate` / `bun run db:migrate` (Drizzle). Tests: `bun test`. Typecheck: `bun run typecheck`.
- DB tests need the test database explicitly: `DATABASE_URL=postgres://$USER@localhost:5432/secure_test_design_tool_test bun test`.

## Dev (the client, from `client/`)
- `cd client/SecureTestCore && swift test` — all the logic. No Xcode, no window server.
- `cd client && xcodebuild -project SecureTest.xcodeproj -scheme SecureTest -destination 'platform=macOS' build`.
- The AppKit/WebKit surface (drag, Touch Bar, menus, file pickers) CANNOT be tested here — no window server, and headless browsers hang (ADR 0013). `client/MANUAL-CHECKS.md` is the hand-check list; keep it current when hardening changes.

## Workflow & conventions
- One slice at a time, commit per slice, wait for explicit "proceed" / push approval.
- Phase 7 (`docs/phase-7-slices.md`) is the current doc of record: sittings, pre-assignment, teacher monitor v2, roster go-live — slices 82–94 DONE plus the AAC-2b follow-ups (3.1–3.3) and File→Open (PR #4), all hand-run. **On-demand peek — Phase 2's last feature — is BUILT and VERIFIED (2026-08-28, `docs/on-demand-peek-design.md`)**: teacher-initiated 1:1, student-notified, client self-render (`cacheDisplay`, finding #14), delete-on-read + audit row; the locked-session cycle is stderr-proven. Findings 8.1 / 8.2 (rebind on join) / 8.3 (deferred `end()` + `SECURE_TEST_SIMULATE_LOCKDOWN=slow`) / 8.5 are BUILT 2026-08-28 (workflow round, merged; hand-run rows in `client/MANUAL-CHECKS.md` "2026-08-28 workflow round"); 8.4 (word_completion still gated with the AAC knob AND `allowsInlinePredictions` open) stays open; follow-ups 9.1 (importer waits for the 0-ACU cluster to resume) + 9.2 (PeekResponderTests race) BUILT and 9.4 (one-AZ endpoint) trimmed the same night — 9.1/9.4 DEPLOYED 2026-08-28 19:40 PT with cold-cluster proof (`docs/phase-7-slices.md:98`; an earlier "still need cdk deploy" note here was stale); 9.3 (re-entrant `end()`) CLOSED 2026-08-28 — the 8.3 real-session hand-run passed (deferred `end()` delivered `DID END`, exit 0); 9.5 accepted. **Student-account sign-in run DONE 2026-08-29** (two demo accounts seeded into the local dev roster; ADR 0017 #2 = YES; 8.2 rows 2/3 ✅ — findings 10.1–10.6 in `docs/phase-7-slices.md`, proposals only). **Aurora run DONE 2026-08-29 night** (real student signed in, joined, handed in on the real import; findings 10.7–10.9: DNS breaks for the dev server on the same Mac during a real AAC session, events not retried, no real enrollment current before 2026-09-02). **8.5 rows ✅ the same night**, and their row 3 exposed + fixed **10.10** (S3 presigned upload slots were never marked complete → drawing answers lost; the server now HEADs the backend and settles the slot). **UX pass 1 BUILT 2026-08-30** (ten slices on `main`: PSD tokens on a Mist ground, app shell, Assessments home, editor header/tabs/Publish, inline item feedback, Test sessions, Monitor, Students, sweep, docs — `docs/ux-pass-1.md` is the record, `docs/ux-pass-1-proposal.md` the research + slice log; the accidental dark-only theme is gone; teacher nouns are Test session / Session code / Handed in / Needs attention / View screen / Students — identifiers untouched, see the glossary). **UX pass 1 hand-run STARTED 2026-08-31** (`docs/design-tool-manual-checks.md`): rows 1, 6, 8, 9, 12, 16, 17 ✅; 2, 5, 7 ❌ → findings P2-1…P2-5 (proposals, listed in that file and in `docs/ux-pass-1.md` §6); 15 ✅ (lazy overlay-row create + select-driven Add support, second pass); client rows 10/13/14 ✅ 2026-08-31 (one student sequentially, simulated lockdown; P2-6…P2-8 added); 18 ✅; 3 + 4 ✅ 2026-08-31 (P2-9 clip confirmed in the real flow); 19 deferred to pre-pilot; 20 at the first cdk deploy, and the pass-2 list lives in `docs/ux-pass-1.md` §6. Pass-2 decisions MADE 2026-08-31 (P2-5 persist, P2-6 keep-for-pilot + copy, 10.4 accepted, 10.7/10.9 record-only) — **UX pass 2 BUILT 2026-08-31** (all 8 slices of `docs/ux-pass-2-proposal.md`: select_account + error-box clip, noValidate, stem select-on-focus + publish locks text, alertIsCurrent rule + closed-monitor polling, Keep-mine persistence w/ migration 0023, client way-home + join copy, lifecycle event retry, Session menu Cmd-E — client rows ✅ incl. the same-day re-check: session-ended sheet, in-page way home, clean Cmd-Q; prompt=login shipped on the client. **Slice 9 WKWebView sign-in BUILT 2026-08-31** (Google session stickiness lived in the AuthenticationServices store; `WebViewAuthPresenter` — own WKWebView, `nonPersistent()` store per attempt, Safari-shaped UA against Google's embedded-webview block — replaces `GoogleAuthPresenter`; hand-run COMPLETE 2026-08-31, ALL rows closed: gate PASSED — Google serves the page to the Safari-shaped UA; sign-in traverses ClassLink SSO + MFA in-sheet (no-allowlist choice load-bearing); staff MFA ✅; NO passkeys on PSD Google accounts; Cmd-Q-mid-sheet beep found + fixed (`AuthSheetWindow` catches it before WKWebView swallows the key equivalent); Web-Content-kill proven; sign-in failures return silently to the sign-in prompt by decision. Follow-on same day, James: **fresh sign-in forced every launch** — session JWT in-memory only, `KeychainTokenStore` deleted + legacy row purged at startup; crash mid-test costs a re-auth (now incl. ClassLink+MFA) before Resume, accepted). OPEN follow-up in the proposal: SittingsPanel hydration stall, Securly suspect. Dev launchers: client/scripts/launch-client.ts + design-tool/scripts/mint-student.ts). **Next up (2026-08-31 planning): the first real-enrollment morning's checklist (internal — see the ops repository) — Aurora `current_date` is UTC, the flip happens 5 PM PT Sep 1; migration 0023 still needs Aurora — and `docs/ecs-deploy-plan.md` (first Fargate deploy — **slices 1–4 BUILT 2026-08-31**, acd37e4/f057e2b/e782bfb/ce3e8eb: vendored-woff2 fonts + standalone + Dockerfile [container-verified vs local Postgres, colima replaces the dead Docker Desktop], ALB'd ARM64 Fargate service + task role + min ACU 0.5, ECS-injected secrets + boot-shim DATABASE_URL [deliberate deviation from the SDK pattern, recorded in the plan doc] + all four provider selectors to bedrock, ACM cert + HTTPS + explicit OIDC_REDIRECT_URI on <origin>; **slice 5 DONE 2026-08-31 late evening — LIVE at https://<origin>**: secret/GCP/0023(count=24)/deploy/CNAMEs all closed; live verify passed (sign-in+sign-out, S3 upload, Bedrock item gen via task role) after finding+fixing the req.url-origin bug (post-auth redirects pointed at the task's internal hostname; lib/auth/appOrigin.ts, 22f82d9) — second deploy also shipped /api/health + SittingsPanel timeouts; still open: client run vs the origin, row 19 pre-pilot).
- Two-terminal convention (2026-08-27): client work happened in a second Claude session on its own `claude/*` branch, landed on `main` by merge/PR; this checkout owns design-tool, DB, infra and docs. One owner for `packages/` and Drizzle migrations; fetch + rebase before every push. That session is closed; its last work is merged (`5cf00a8`).
- AI model wiring is live: Claude Sonnet 4.6 (item gen) + Haiku 4.5 (math) on Amazon Bedrock via SigV4 (ADRs 0007/0011); `mock` stays the default for tests/CI. S3 storage is live (ADR 0008), including presigned client uploads; local-fs remains the dev default. Identity is **Google OIDC** (ADR 0017, slices 77–81): role from the verified email domain (`psd401.net` staff, `edtools.psd401.net` student), `OIDC_AUDIENCE` lists the web and native client ids, the client signs in through `ASWebAuthenticationSession` (`SECURE_TEST_GOOGLE_CLIENT_ID`) and keeps `--token` / `SECURE_TEST_TOKEN` for dev/CI. Roster is a nightly warehouse extract into `roster_*` (`docs/roster-extract.md` is the data engineer's contract; `lib/roster/`); `students` is the per-teacher accommodations overlay joined on `roster_ps_id` / SSID. Nothing ClassLink remains in code. Mark remaining deferred wiring as "TODO" in docs.
- Two wire formats, deliberately (ADR 0016): `ItemBundleSchema` is the teacher's export and CARRIES the answer keys (import round-trips through it); `DeliveryBundleSchema` is the student's and has no field that can hold one. Never merge them. Match/order additionally carry per-attempt sealed option ids, because their key is carried by structure rather than by a field.
- Accommodations source of truth: TIDE import is canonical per-bundle (re-import overwrites); teachers may then edit (IEPs change); manual entry also allowed.
- External state (2026-08-27 evening): **The data engineer replied** (internal — see the ops repository) and our side is done (slices 88–90: list-shaped manifest accepted, `ps_id` = student number, blank `teacher_email` documented, bucket policy for their MWAA role + BucketOwnerEnforced deployed to dev); our reply was sent — **first snapshots LANDED 2026-08-28** (daily 06:00 PT push works, `ssid` filled) and the importer now runs inside the VPC (AI Studio pattern, deployed to dev 2026-08-28) — the first real import on Aurora succeeded on S3's retry after the 0-ACU cluster resumed (follow-up 9.1); staff sign-in is live-verified in the browser and the native client; the **student-account run is DONE 2026-08-29** (demo accounts locally, the real student on Aurora). (The Apple AAC entitlement **cleared** — notified 2026-08-26, verified in PoC-A's signed binary, `begin()` succeeds on macOS 26.6.2.)
- AAC lockdown is now buildable, and carries one hard rule learned the expensive way: **anything that calls `AEAssessmentSession.begin()` ships an exit path in the same change.** PoC-A's first session had none and the Mac needed a forced shutdown; the exits (button label, Cmd-E/Cmd-Q menu, window close, trapped SIGTERM, and a non-disableable watchdog) went in 2026-08-26. Watchdog, button, Cmd-E and Cmd-Q are verified inside a real session (2026-08-26, RESULTS finding #12); window close, SIGTERM and escalation are simulation-only. Screen Recording was granted to the sandboxed `PocA.app` by an IT admin toggle on 2026-08-27 (TCC keys on code identity — a rebuild that changes signing or sandbox can strand the grant on the old row again), and the capture measurement ran the same day (PoC-A RESULTS finding #13): ScreenCaptureKit works inside a session but the assessment window comes back as a flat grey box, `allowsScreenshots` true or false — screen-capture-based thumbnails/peek are dropped. An in-process render (`NSView.cacheDisplay`, finding #14) survives the session with full content, so live monitoring rebases on the client rendering itself and pushing the image (plan 6.16 resolved). Checklist steps 5–6 measured (finding #15): system UI suppressed; AAC gates a secondary app's *visibility*, not its launch. PoC-A has no open items. TCC attributes a terminal-launched app to the terminal, so shell-launched runs need the terminal granted too. **A real session also breaks DNS for every OTHER process on that Mac** (finding 10.7, 2026-08-29): a dev server running beside the locked client cannot resolve Aurora or S3 until `DID END` — saves 500, the spool holds them, hand in again after the session; localhost traffic is unaffected. The shipping client is expected to need NO Screen Recording grant (self-render monitoring; confirmed by the signed build, D-R4). Remote Login is CLOSED (solved another way, 2026-09-04). The batched IT afternoon's list (internal — see the ops repository) is revised 2026-09-04: the Developer ID profile + certificates for the client, the dictation Jamf profile, the test-fleet scope. Student IdP pivoted from ClassLink to Google (ADR 0017; plan.md 6.17 resolved).
- **State as of 2026-09-03 late afternoon — start here in a fresh session.** LIVE at https://<origin> = origin/main `3d725f8` (rev 10, Aurora at 0027; nothing deployed since). **The client-fixes batch's hand-run is DONE** (`docs/client-fixes-batch-2026-09.md` §Progress is the record): one sitting on the origin with the demo student (James at the client, simulated lockdown; Claude on the teacher side in Chrome) on the fixture `Client-fixes hand-run 2026-09-03` (`0e1108d6…`, imported from a scratchpad bundle — every item type, three drawings, a keyless table, paged; **left on the origin** for the F-1 / hotspot work, delete after) — row 57 ✅, 27 client rows closed except three not-exercisable cells and the older-client row, the offline rows ✅ via Cmd-O on the harness fixture; **row 58 NOT run** (needs local dev + a minted token; the client's behaviour covered the bundle). Drawings were verified by reading the S3 objects (`responses/<attempt>/<item>/<uuid>` in `secure-test-design-tool-dev`) with `aws s3 cp` — F-1 stands. **Findings, all placed in `docs/roadmap-2026-09.md` ("Findings from the 2026-09-03 sitting"), nothing built:** a new **batch 0b** (the hotspot renderer has NEVER had CSS — regions are grey buttons under the picture, a shipped defect; the short-text math preview should render for every answer, not only on `_ ^ $ \`; match should be marked answered only when every pair is set), predictive text in the essay under simulated lockdown → batch 2's real-session row (finding 8.4), order as drag-and-drop → batch 4. The fixture's missing hotspot PICTURE was a hand-pasted base64 that lost bytes (stored 2741 of 3356) — WebKit refuses a corrupt PNG, Chrome draws part of it; **import fixtures from a file, or verify the stored size**. The demo student's name / number never go into tracked files (`git grep` before committing). The doc close-out (both check files, four design pages, the roadmap, this bullet) is one commit, pending James's go-ahead at the time of writing.
- **State as of 2026-09-03 midday (superseded above, kept for the record).** LIVE at https://<origin> = origin/main `3d725f8` (**deployed 2026-09-03 12:01 PT, task def rev 10, rollout COMPLETED, /api/health 200; NO migration — Aurora stays at 0027**; the client rebuilt from `3d725f8` in DerivedData `SecureTest-gwoymhsfucmcdzaznczoqsmmosaf` carries `clear-answer` / `saved_responses` / `data-background` / `paintBackground`). Local `main` = origin. What that deploy carried — **the whole client-fixes batch, `docs/client-fixes-batch-2026-09.md` is the record, all five items BUILT 2026-09-03 and NONE hand-run yet:** 9f89a51 (merge of E3-F1 from the second terminal: a keyless table's effective method is `human`, flips to `auto` on the first key, the queue lists keyless tables — row 57), d130922 (#4 the essay box finally has CSS: 240 px min-height, vertical resize — 3 client rows), 92b8d27 (#3 "Clear answer" on MC single + multi over the existing spool withdrawal → `DELETE`; `ANSWERED` un-mark — 5 client rows), 53855cb + 132929a (**P-1**, `docs/resume-prefill-design.md`: the bundle carries `saved_responses` + `saved_uploads`, one pass with `answered_item_ids`, match / order re-sealed, drawing bytes ≤ 2 MB inline; the client restores every field silently and paints the saved drawing — row 58 + 10 client rows), bfa6140 + 3d725f8 (**drawing background**, `docs/drawing-background-design.md`: `canvas.background: grid | axes` in the shared schema rides every pass-through, editor select, preview / print graph paper; the client paints the paper into the canvas before strokes and after Clear — 9 client rows). Finding **F-1** (P-1 page): a teacher cannot view a saved drawing — the queue renders it blank, nothing reads `response_uploads` back; unbuilt. How the batch ran: 1a in a second terminal (Sonnet 5 / medium, worktree `claude/e3-f1`, merged and removed); the client line sequentially from the main session via subagents with the `model` override (Sonnet 1b, Opus P-1 + background), each diff reviewed and each suite re-run in the main session before its commit; a fixture regeneration churns every id, so tests read labels off the bundle (RendererPrefillTests was corrected for that). Test counts at the end: schema 114, design-tool 1119, client 362.
- **State as of 2026-09-02 evening (superseded above, kept for the record).** LIVE at https://<origin> = origin/main `bf69cac` (**deployed 2026-09-02 20:38 PT, task def rev 9, rollout COMPLETED, /api/health 200; Aurora at migration 0027** via migrate-aurora the same minute; the client rebuilt 20:29 PT in DerivedData `SecureTest-gwoymhsfucmcdzaznczoqsmmosaf` carries `outline-inline` / `fill-table` / `pager-strip` / `answered_item_ids`). Local `main` = origin. What that deploy carried, all built 2026-09-02 and NONE of it hand-run yet: f492615 (a panel import shows without a reload), bc57fa4 (Bold / Italic buttons), 980c9bb (imported copy named "(copy)"), 7ff3b73 / 3548ffd / c30db51 / 4a06c09 = E12 slices 1–4 (`item_sets.source_item_id` + **migration 0026**, per-student delivery resolution, the client's writing area, the editor picker + preview placeholder + review-queue indicator; `docs/e12-per-student-stimulus-design.md` is the record), then **E3 table item BUILT end to end 2026-09-02** — a1945ca / 11dcef9 / c932786 / 40bdbac + the rows commit = slices 1–5 (`docs/e3-table-item-design.md` §Progress: schema + API + per-cell scoring + bundles + preview; editor form + readiness + review-queue grid + hand-scored table worth its cells; the client's `tableField` + regenerated Swift fixture; PDF-import prompt shape + pipe-table backstop, Bedrock re-run of Unit 0 proposes a `table`; rows 48–53 + client rows written, NOT run). No migration. **Ships in the same deploy as the E12 client rebuild** — the installed client refuses a bundle with an unknown item type. Then **client paging BUILT end to end 2026-09-02** — 1b939ef / 3fc70f4 + the rows commit = slices 1–3 (`docs/client-paging-design.md`, D-1…D-5: a per-assessment `student_layout` setting, default scroll, **migration 0027**; the client pages only on an explicit `"paged"` — own_page = a passage page then one page per question with the passage collapsed above it, inline set = one page, a review page last, Previous / Next + jump strip; plus the **answered-marks follow-up BUILT the same night** — the delivery bundle's `answered_item_ids`, green / amber strip buttons and "k of N answered" on the review page; rows 54–56 + client rows written, NOT run). `migrate-aurora` now runs 0026 AND 0027; the client rides the same rebuild. **Deploy sequence DONE 2026-09-02 20:41 PT** (push → diff: image + importer bundle only → deploy → migrate-aurora 0026 + 0027 → client rebuild; the AWS SSO token had expired and James re-logged in with `aws sso login --profile <your-sso-profile>`). **Teacher-side hand-runs DONE 2026-09-03 morning (Claude driving Chrome on the origin): rows 43, 44, 45, 46, 48, 49, 50, 53, 54 ✅** — results in `docs/design-tool-manual-checks.md`. **Student sitting DONE 2026-09-03 late morning** (the replacement account arrived; James at the client, Claude on the teacher side): rows 47, 51, 52 (auto half), 55, 56 ✅ by behaviour; the client rows for E3, paging, answered marks and E12 slice 3 ✅ (`client/MANUAL-CHECKS.md`). **Two findings:** **P-1 DECIDED — high-priority build next:** marks without prefilled fields read as lost answers → the delivery route sends saved responses and every field restores its value (`docs/client-paging-design.md`); **E3-F1 DECIDED (James, 2026-09-03), not built:** a keyless table defaults to hand-scored and flips to auto when a key is entered (`docs/e3-table-item-design.md` has the scope). The hand-run fixtures (draft, copy, Outline) were deleted from the origin after the run; the E6 / E7(b) client rows stay open (James: nothing depends on them). The client is already rebuilt (`xcodebuild …`, verify `SecureTest.debug.dylib` carries `outline-inline`, `fill-table` and `pager-strip`) for the client rows (E6 / E7(b) / E12 / E3 slice 3 / Client paging in `client/MANUAL-CHECKS.md`). **Waits on James:** replacement student credentials (district-wide reset 2026-09-02) for the client rows E6 / E7(b) / E12 in `client/MANUAL-CHECKS.md` — recipe there; the one-day teacher-row script lives OUTSIDE the repo at `~/secure-test-hand-teacher-row.sh` (it names the student; re-run on the day, the row expires at the 06:00 import). **Student numbers never go into tracked files** (real one scrubbed from HEAD in 1dc4383; demo numbers are fine). On the origin on purpose: `E6 / E7(b) hand-run 2026-09-02` (Published; delete after the client run), `Unit 0 - clean PDF import 2026-09-02 (no edits)`, `Chemistry sample`, `test`, `Test 2`, the seed; the other hand-run artefacts were deleted 2026-09-02. Hand-runs 2026-09-02 against the origin: rows 37–42 ✅. The SAMPLE-IMPORT drafts are gone. Everything below shipped on 2026-09-01 and each has its own record — read the record, not git history: slices A–C (structured PDF-import errors, keyless items, staff sharing with copy semantics) and PDF-import slice 2 (E1 match, E2 drawing, E4 show-your-work → essay + "Final answer:", E7a KaTeX in stems/choices/pairs never in keys, E8 numbering shortfall) in `docs/pdf-import-enhancements.md`; the infra slice (Aurora migrations run in-VPC via `design-tool/infra/scripts/migrate-aurora.sh`, laptop CIDR gone from the cluster SG, OAuth ids in `infra/cdk.json`) in `design-tool/infra/README.md` + `docs/ecs-deploy-plan.md`; **E5 stimulus / item sets built end to end** (decisions, spikes S1/S3, slices 1–4: schema 0025 + API + editor + both bundles + preview, client rendering, figures with `[FIGURE n]` markers, model pairing + adjacency fallback + set cards) in `docs/stimulus-design.md`; the client now renders KaTeX (`docs/adr/0009-katex-math-rendering.md` addendum). Twelve `SAMPLE-IMPORT:` drafts still sit on prod under James's account — **delete them when done reviewing**.
- **Roadmap after the rows: `docs/roadmap-2026-09.md`** (written 2026-09-03, decisions made, nothing built) — batches: 1 scoring hygiene (`docs/reporting-design.md` R0) → 2 package + sign the client (`docs/client-release-plan.md`; the client has NEVER been released; the generic `psd-sign` re-sign strips the AAC entitlement — fix the skill) ∥ 2b the public repository (`docs/public-release-plan.md`; development MOVES there, this repo is archived, a private ops repo holds internal notes) → 3 observability (`docs/observability-design.md`; in-house, SNS email) → 4 client UI pass (`docs/client-ui-pass-design.md`; theme layer, then the unimplemented contrast / font / zoom accommodations) → 5 reporting views + gradebook CSV + print report → 6 gradebook integration (PowerSchool official, Schoology covered).
- **Batch 0b BUILT 2026-09-03 evening** (`ef48d7c` hotspot CSS — regions invisible until hovered / focused, filled when selected; `ac85588` math preview on every short-text answer, spaces kept; `374a065` match marked answered only when every pair is set, restored partials start unmarked; `swift test` 365, `xcodebuild` green; the app in DerivedData `SecureTest-gwoymhsfucmcdzaznczoqsmmosaf` is rebuilt from it). **PUSHED 2026-09-03 evening** (`bbd6d52` close-out + the three slices + the CLAUDE.md update); nothing to deploy (client only — the origin stays at rev 10 / `3d725f8`). The 14 rows ("Batch 0b slice 1–3" in `client/MANUAL-CHECKS.md`) are NOT run: a five-minute sitting on a Published paged assessment with a three-pair match, a short text and a hotspot carrying a real PNG (the sitting fixture's is corrupt — add a hotspot with an Images-page asset).
- **Batch 1 scoring hygiene BUILT 2026-09-03 evening** (`eb2c969` auto-score on submit via the shared `lib/scoring/runAutoScoring.ts`; `df899b2` F-1 — `GET /api/responses/[responseId]/upload` owner-only + `<img>` in the queue; `7d9a3e0` identifiers on results — D-R1 / D-R2 ACCEPTED, CSV header `student_number,name,email,section,submitted_at,Q1..Qn,total,max,percent,unscored`; design-tool 1132 pass, typecheck clean; no migration). **Rows 59–61 ✅ 2026-09-03 evening on the origin** (60/61 Claude in Chrome on the client-fixes fixture; 59 a fresh sitting on `E6 / E7(b) hand-run 2026-09-02` with the demo student — the client-fixes fixture already had that student's one attempt). **PUSHED + DEPLOYED 2026-09-03 evening: LIVE at https://<origin> = origin/main `ea91b49`, task def rev 11, rollout COMPLETED, /api/health 200, no migration (Aurora stays at 0027)**; James ran the deploy by hand (classifier blocks `cdk deploy`; SSO re-login first). `docs/reporting-design.md` §Progress is the record.
- **Batch 2 slices 1 + 3 BUILT + PUSHED 2026-09-03 late evening** (`73fe09f` project prep: 1.0.0, "Secure Test" display name, hand-written `Assets.xcassets` — white emblem on a Pacific tile, Cedar accent — the `PSDBuildCommit` stamp phase [needs `ENABLE_USER_SCRIPT_SANDBOXING = NO`, build-time only], `AppVersion.buildStamp` + About, Whulge peek strip, Pacific sign-in header, Release = Manual / Developer ID / placeholder profile `SecureTest Developer ID`; nine branding rows in `client/MANUAL-CHECKS.md` unrun. `9e74abf`: `~/.claude/skills/psd-sign/SKILL.md` is 0.4.0 — keeps entitlements, mandatory pre-notarization entitlement diff, profile check, `--repo`; James is its only user, it stays unversioned). **Slice 2 DONE 2026-09-07**: IT sent the profile (`SecureTest Developer ID`, AAC, expires 2044; copied by UUID into Xcode's UserData profile store — double-click did not install it); archive → export developer-id → notarize (keychain profile `notarytool`) → staple all passed with no project change; the real AAC session ran on the notarized app against the origin (`Chemistry sample`, demo student): `REAL AEAssessmentSession` → `DID BEGIN` → hand in → `DID END`, **no TCC prompt (D-R4 confirmed, no PPPC needed)**; 8.4 predictive text still OPEN (not watched). Gotchas: `SECURE_TEST_SIMULATE_LOCKDOWN` sits in `design-tool/.env.local` and the launcher forwards it — blank it on the command line for a real session; an attempt is unique per assessment + student with NO delete path (roadmap finding 2026-09-07, batch 5). IT also has the dictation DDM declaration ready (macOS 26.4+) and awaits the fleet-scope answer; the one-day teacher-row script still works. **Slice 4 DONE 2026-09-07: `v1.0.0` PUBLISHED on `psd401/secure-test`**; **v1.1.0 PUBLISHED 2026-09-08 ~15:20 PT** (built from `0e52d02`, tag on `main`; batch 3 client + batch 4 + the sitting's fixes; `/releases/latest` = 1.1.0). Open: the hand-install row on a district Mac, IT's fleet scope (`SecureTest-1.0.0.pkg`, Installer-signed + notarized + stapled, `/releases/latest/download/` answers 302; tag on `main` = `c719eb5`); `client/RELEASING.md` is the checklist. Slice 5 DRAFTED 2026-09-07 in the ops repo (reply to IT + Installomator label + Jamf policy ask; fleet scope still James's; floor stays macOS 26.5). **Batch 2 COMPLETE except the hand-install row on a district Mac.** **Batch 3 observability BUILT 2026-09-07, slices 1–4** (`docs/observability-design.md` §Progress is the record): `9d7231d` infra (30-day log group, `secure-test-notify-<env>` topic + email from context key `notifyEmail`, four alarms — metric filter on `level:"error"`, ALB 5xx, ALB HealthyHostCount, importer Errors); `a58db94` server (migration **0028**: `server_error_events` / `client_error_events` / `feedback` + `client_error` attempt-event kind; `lib/log.ts`; `instrumentation.ts` `onRequestError`; request id in `proxy.ts` — matcher now covers every non-static path; root + global error boundaries; `POST /api/client-errors`); `031fb75` client (`errors.log` sink, pre-formatted signal-handler crash lines, drain after sign-in, `client_error` event, `SECURE_TEST_DEBUG_CRASH=1` menu item); `d863a1b` feedback (`lib/notify/` mock|sns, `POST /api/feedback`, header dialog, `NOTIFY_PROVIDER=sns` from infra). Design-tool 1180 / client 387 tests. **DEPLOYED 2026-09-07 ~09:57 PT: rev 12, /api/health 200 + `x-request-id`, Aurora at 0028** (`6dc7418` gated `onRequestError` to the Node runtime after the first build's Edge warning; `cdk deploy --require-approval never` because a backgrounded `!` has no TTY). **Teacher-side rows 62–68 ✅ 2026-09-07** (feedback round trip incl. the email; alarm path proven with a synthetic log line → ALARM in 77 s; row 67 open — no reachable input throws on the origin). **SNS gotcha:** the district mail scanner clicks the Unsubscribe link in every SNS email — the subscription is now CLI-confirmed with `--authenticate-on-unsubscribe true` (infra README) and lives outside the stack. `.env.local.example` has the NOTIFY_* lines (`2c8e822`). **Open:** the client rows (`client/MANUAL-CHECKS.md` "Observability slice 4", James at the keyboard, `SECURE_TEST_DEBUG_CRASH=1` for the crash row; rebuild first), server build stamp (`APP_COMMIT`), a retention sweep for the four event tables, the orphaned old log group. **Batch 4 client UI pass BUILT 2026-09-07 evening, slices A–E** (`docs/client-ui-pass-design.md` §Progress; decisions D-A1/A2, D-B1 Atkinson, D-B2, D-B3 streamlined zoom clamped, D-B4 three contrast pairs adjusted for 4.5:1, D-D1 AppKit branding, D-D2 drag-and-drop): A theme tokens + Inter/Josefin inlined; B eight contrast sets + Atkinson + nine zoom levels from `ACCOMMODATIONS`; D entry screen on Auto Layout (white card on Pacific, emblem, two-line rows, Whulge primary buttons, `contentMinSize` 720×620, notice pages on tokens); E order drag-and-drop with the Move buttons kept. swift test 448 after the sitting's fixes, merged at `9e0356e` and pushed. **Client sitting DONE 2026-09-08 (three passes, real AAC sessions, `client/MANUAL-CHECKS.md` "Client sitting 2026-09-08" + `docs/client-ui-pass-design.md` findings S-0…S-9):** the HTML5 drop failed under `LockedDownWebView` → pointer tracking (S-1) built and proven; S-0 = a slice-D constraint activated before `addArrangedSubview` threw inside the sign-in Task and silently killed the main actor (lldb's thread name "Task N" on an idle main thread is the tell; `a755671`); S-2…S-5b, S-8 fullscreen by default (`SECURE_TEST_NO_FULLSCREEN=1` for dev), S-9 built and confirmed; finding 8.4 CLOSED (no predictive text in a real session); batch 3 slice 4's crash + drain rows ✅ (the launcher forwards `SECURE_TEST_DEBUG_CRASH`). S-6 keypad + handwriting spike and S-7 drawing tools → roadmap 4b / 4c (fresh sessions). Client only: no deploy. **Fixtures `Client rows hand-run 2026-09-08` + `(copy)` stay on the origin (James) — they carry the day's attempts.** **Fixture for the client rows: `Client rows hand-run 2026-09-08` (Published on the origin 2026-09-08, `17f0f4a6…`; MC, order, 3-pair match, KaTeX short text, hotspot with a real PNG, essay, drawing grid + axes, table, multi-select; paged) — imported from a file built by `design-tool/samples/_build-client-rows-fixture.ts` (gitignored); serves batch 0b + batch 3 slice 4 + batch 4 rows in one sitting; re-run the one-day teacher-row script first; delete after.** **Batch 5 reporting R1 + R2-print BUILT + merged 2026-09-07 evening** (`78b1638`, `docs/reporting-design.md` §Progress; D-R3 gradebook CSV WAITS for sample PowerTeacher Pro / Schoology exports, D-R5 owner-only): `GET /api/attempts/[attemptId]/events` (staff GET beside the student POST — auth-role-enforcement has an explicit per-method map), `lib/reporting/{timeline,analytics,answerView,printIntegrity,printSummary}.ts`, `/dashboard/[id]/results/[attemptId]` per-student page with the integrity timeline, results matrix section filter + Complete marker + analytics footer + Results button on the list, `/dashboard/[id]/results/print` (`?section`, `?attempt` = family-facing single page). 1245 tests, no migration. **DEPLOYED 2026-09-07 21:39 PT: task def rev 13; then rev 14 on 2026-09-08 ~10:55 PT with the print-report fixes P5-1…3 (`45adb33`), health 200; teacher rows 69–74 + R2-P1…P6 ✅ / partial as recorded** (the CLI itself exited 1 with `SignatureDoesNotMatch: Signature expired` while monitoring — a hung 300 s request, the stack finished regardless; verify with `describe-stacks` / `describe-services` when that happens); print summary → `analytics.ts` DONE in the P5 fix. **State at close-out 2026-09-08 ~15:20 PT — start here:** LIVE = rev 14 (`45adb33`); client **v1.1.0 published** (`0e52d02`); batches 0b, 1, 2 (bar hand-install), 3 (bar row 67 + the in-attempt `client_error` row), 4 (bar VoiceOver + per-pair screenshots), 5 (bar the gradebook CSV awaiting sample exports and the section-filter / other-teacher rows) DONE. **Delete attempt BUILT 2026-09-08 afternoon, two commits (`bb0c205` + `d64482c`), PUSHED, NOT deployed** (`docs/reporting-design.md` §Progress is the record): `DELETE /api/attempts/[attemptId]` owner-only + 409 `session_open` for an in-progress attempt on an open sitting, cascades + best-effort storage delete + audit row in `attempt_deletions` (**migration 0029**, applied to dev + test; Aurora needs `migrate-aurora.sh` after the deploy), `DeleteAttemptControl` on the per-student results page and every joined monitor row (disabled until the session is closed; monitor column now "Actions"); rows 75–78 in `docs/design-tool-manual-checks.md` unrun; James passed the releases link to IT the same day and is recruiting pilot teachers. **Next session, in order:** (0) push + deploy + migrate 0029, run rows 75–77; (1) hand-install `SecureTest-1.1.0.pkg` on a district Mac and run one real session from `/Applications` (closes batch 2); (2) send IT the fleet scope, then the Installomator label + Jamf policy from the ops repo; (3) roadmap **4b** math entry (fresh session: design note Fable / high → keypad Opus / medium; handwriting spike Opus / high) — **4c is BUILT, see below**; (4) the gradebook CSV when the sample exports arrive (Sonnet / medium); (5) batch 6 later. Working rules learned 2026-09-08: worktree agents branch at LAUNCH time — launch dependent slices after the merge, or expect stylesheet/doc hunks to rebase by hand; an ObjC exception inside a main-actor Task does not crash, it silently parks every later main-actor continuation (lldb: main thread named `Task N` with no task frames); the hidden automation tab needs a screenshot before clicks; `form_input` on a React checkbox can skip React state — click from a fresh screenshot instead. **Roadmap 4c drawing tools BUILT + PUSHED 2026-09-08 evening — `docs/drawing-tools-design.md` is the record (origin/main = `ff8611d`).** Four commits: `7d878ad` the design note (D-1…D-8 decided by James the same day: three sizes 1.5 / 2.5 / 5 with the eraser 4×, four inks, eraser = `destination-out` + the paper re-painted under with `destination-over` on every move, Clear not undoable, Cmd-Z scoped to the item / no Redo, a screen-only `--canvas-paper` white ground so a blank canvas is drawable under a dark contrast set while the PNG stays transparent, Undo at the end of the strip, auto-save as its own slice); `2cba37e` slice 1 — the `role="toolbar"` strip above the canvas, a per-canvas stroke list replayed by `rebuild()` / `undo()`, the previously missing `.drawing-controls` / `.drawing-status` CSS, `canvas.__pen` for the paint's pen hand-back; `5530cee` slice 2 — idle-debounced auto-save (5 s, single flight, flushed on page turn via the pager's `show(n)`, on focus leaving the item and on Finish before the submit post; off offline; the manual button on the same path) + the Core `UploadGate` actor the host uses so a hand-in waits (20 s) for in-flight drawing uploads and otherwise refuses with `submit refused: N drawing upload(s) still in flight`; `ff8611d` slice 3 — 35 rows in `client/MANUAL-CHECKS.md` "Drawing tools — pen size, colour, eraser, undo, auto-save (2026-09-08)", NONE run. Client only, no deploy; `swift test` 489, `xcodebuild` green; ships in the next client release. Three recorded readings (note §Progress): a manual Save during an upload that then fails waits for the next change; Clear of a saved picture uploads nothing (the server keeps the last picture); the gate's begin / wait ordering is FIFO in practice — a `@MainActor` gate is the fix if a hand-run ever shows a submit overtaking an upload. **Next for 4c: rebuild the client in a separate session (James), then the rows** — the 2026-09-08 fixture plus a blank and a grid drawing, the one-day teacher-row script, a real AAC session for the Cmd-Z and hand-in rows. Working rules learned: an interrupted subagent cannot be resumed (`ListAgents` does not show it) — relaunch with "finish the partial tree, do not discard"; the Opus-per-build-slice / Sonnet-for-rows pattern with the diff reviewed and both checks re-run in the main session held for all three slices.
- **Roadmap row S — multi-source stimulus — slices 1–5 BUILT + MERGED 2026-09-09, `docs/multi-source-stimulus-design.md` is the record (D-1…D-6).** Trigger: the first pilot assessment (an AP Seminar-style free-response PDF: one essay prompt, four labelled sources, four vector charts) imported as the essay alone — three causes measured in the note. Built the same day, one commit per slice, Opus 5 agents per build slice with diffs reviewed + checks re-run in the main session: `d20eeb9` slice 1 `white-space: pre-line` on stems + stimulus bodies (client, preview, editor live preview); `ed59110` slice 2 **migration 0030** (`item_sets.sources jsonb` `[{label, text}]` ≤ 12, layout CHECK + `ITEM_SET_LAYOUTS` gain `side_by_side`), `StimulusSourceSchema` on `ItemSetSchema.sources` on BOTH bundles (default `[]`), create/PATCH take `sources` whole, export/import/delivery/preview scan source texts for `asset:` refs, preview `<section class="source">` blocks, readiness `Source "B" for question N is empty`, the editor's Sources list; `f556300` slice 3 import prompt (single-question sets, sources printed after the prompt, labelled sources verbatim as `sources`, chart text belongs to the figure), `flagShortenedSources` vs the document's span under each heading (page furniture on ≥ 3 pages filtered), panel source list + "Looks shorter than the document — check it" badge + layout select, Add posts `sources` + `layout`; `9a52b06` slice 5 vector charts — painted `constructPath` boxes clustered (12 pt, ≥ 8 paths, ≥ 40 pt), page rendered once via unpdf `renderPageAsImage` + `@napi-rs/canvas` 1.0.9 and each cluster cropped, `PdfFigure.source: raster|vector` + `caption` (nearest bold line above, wrapped titles joined), markers KEPT inside a source's text and turned into `![caption](asset:uuid)` at Add, `serverExternalPackages`, **Dockerfile copies `design-tool/node_modules/@napi-rs` from the builder because standalone tracing writes no link for it** (S4 container proof: the route-dir require loads as `node`); merged from `5fe50fb` slice 4 client (second terminal, worktree `claude/multi-source-client`): `StimulusSource` decode, tab pane with the keypad's roving tabindex, `side_by_side` two columns ≥ 1100 px at build (D-6: read once, CSS stacks on shrink), own_page fallback with "Show the sources". Tests: schema 121, design-tool 1366, client 529. **PUSHED + DEPLOYED 2026-09-09 ~11:38 PT: LIVE at https://<origin> = origin/main `200653395d` + the deploy record, task def rev 16, rollout COMPLETED, health 200, Aurora at 0030 via `migrate-aurora.sh`** (James ran the script by hand; `cdk diff` = image + importer bundle only). Bedrock on the pilot after slice 5: 1 essay, 1 set, sources A–D at 99.9 / 98.6 / 93.5 / 96.4 % of their spans (none flagged), 4 vector figures with the printed titles as captions inside Source C. **Teacher rows 79–92 ✅ 2026-09-09 (row 90 half; fixture `Multi-source hand-run 2026-09-09` Published on the origin for the client rows).** **Client sitting RUN 2026-09-09 evening (two passes, real + simulated; 18 of 22 ✅; fixtures `Multi-source hand-run 2026-09-09` + `… (accommodations)` stay on the origin with their attempts).** **Findings C-1…C-7 in the note §Progress — C-1 (real-session build reads a narrow viewport → own_page fallback; fix = evaluate at first layout after DID BEGIN + a student "beside / above" toggle) and C-2 (`$57,600` in prose renders as KaTeX — `$` before a digit must not open math, both renderers) are HIGH and next; C-4 chart zoom (allowsMagnification + click-to-enlarge) = James's decision; C-6 Accommodations-tab checkboxes look saved without Save.** **Decisions D-7…D-9 made 2026-09-09 evening (both zoom parts, autosave, fullscreen stays) → roadmap row S-f BUILT 2026-09-09 evening, all five, one commit each (`508d7d6` C-2: `$`+digit never opens KaTeX in both design-tool tokenizers + the client's OWN math pass — auto-render no longer inlined — + the importer writes `\$57,600`; `8f9f09a` C-1: Core `PageLoadGate` — the page builds after lockdown settles, 5 s backstop, gate opens on every state but `.starting` — + the "Sources: Beside | Above" group on side_by_side pages; `e61d95b` C-6: `lib/autosave.ts` + the Accommodations tab autosaves ONLY the two sets, Settings Save no longer sends them; `9c7d28c` C-7 launcher forwards `SECURE_TEST_NO_FULLSCREEN`; `f6bfaa6` C-4: `allowsMagnification` clamped 1×–3× via KVO + `ZoomLevel`, Session menu ⌘0/⌘=/⌘-, click-to-enlarge overlay on stem / stimulus / source pictures with "<source label> — <alt>"). Tests: design-tool 1386, client 568. Rows written (`client/MANUAL-CHECKS.md` "Row S-f", `docs/design-tool-manual-checks.md` rows 93+), NONE run. **PUSHED + DEPLOYED 2026-09-09 ~18:50 PT: LIVE at https://<origin> = origin/main `09a0b65`, task def rev 17, rollout COMPLETED, health 200, no migration (Aurora stays at 0030); the client is rebuilt from `09a0b65` (markers `renderMathIn` / `layout-toggle` / `image-overlay` / `page load gate` in the dylib). **Row S-f sitting RUN 2026-09-09 evening (`a6861c2`; real session; client 27/41, teacher 93–106 + 108 ✅, row 96 on Bedrock; pinch snap-back = WebKit's own bounds; finding S-f-1 cosmetic). Client v1.2.0 PUBLISHED 2026-09-09 ~20:27 PT** (tag = `3a63809`, `/releases/latest` = 1.2.0; the psd-sign skill from step 2a; `gh release create` by James). Fixture on the origin: `Row S-f hand-run 2026-09-09` (one attempt, keep); the C-6 scratch draft was deleted 2026-09-09 (no UI delete exists — `DELETE /api/assessments/<id>` from the signed-in page). **STATE AT CLOSE-OUT 2026-09-09 ~20:40 PT — start here in a fresh session:** origin/main = `f4a6e2c`; LIVE at https://<origin> = rev 17 (`09a0b65` image, Aurora 0030, nothing pending); client **v1.2.0 published** (tag `3a63809`; the psd-sign skill runs from step 2a in-session, only `gh release create` needs James). Next, in order: (1) the next design-tool deploy carrying three XS slices — S-f-1 (PDF proposal cards print the raw `\$`), `APP_COMMIT` build stamp on `/api/health`, and the roadmap's new **delete-draft** action (Settings tab, Draft only, confirm dialog; the DELETE route + its Published guard already exist — Sonnet 5 / medium); (2) the 14 unrun S-f client rows + batch 0b's 14 rows + the two VoiceOver rows in one sitting on the released v1.2.0 build (fixture `Row S-f hand-run 2026-09-09` stays on the origin with one attempt; re-run the one-day teacher-row script first); (3) the hand-install row on a district Mac (closes batch 2) + IT's fleet scope; (4) then roadmap 4b-f / batch 6 / pilot feedback. Working rules learned 2026-09-09: Chrome's `find` reports checkbox state unreliably — screenshot for state; the classifier denies `gh release create` and the SG/secret-reading teacher-row script ran fine from the session; WebKit's own pinch bounds are 1×–3× (our KVO clamp never fires); the `slow` simulator value is a 2 s begin, not a way to hit the 5 s gate backstop.** Pattern held: Opus per build slice, Sonnet for C-6 + rows, diffs reviewed + checks re-run here before each commit; two fixes of mine on agent output (C-2: a prose-only `\$` run was skipped; C-4: paper-on-scrim caption vanished under dark sets).**, the runner image has no system fonts (a chart with real-text labels may rasterise blank — `fonts-dejavu-core` if seen), the next client release carries slice 4 (until then a v1.1.0 client drops `sources` and renders `side_by_side` inline). Gotchas: colima shares `$HOME` only — a bind mount from `/private/tmp` is empty inside the container; the fixture generator needs `DESIGN_TOOL_DELIVERY_SECRET`; the pilot PDF copy + spike scripts sit gitignored in `design-tool/samples/`.
- **LIVE as of 2026-09-08 ~20:25 PT = task def rev 15 (delete-attempt + migration 0029 on Aurora, math-entry slice 1a), health 200; origin/main = `39aa0fc` + this close-out. Gotcha: colima does not restart after a reboot — `colima start` before `cdk deploy`, or the image build fails with "failed to connect to the docker API". Rows 75 ✅ / 77 half / 71 ✓-Complete-seen 2026-09-08 evening (Chrome on the origin; the `(copy)` fixture's attempt deleted for row 75 — that fixture now has NO attempts; the main 2026-09-08 fixture keeps its record); 77's open+in-progress half needs a live client sitting on a current roster day; row 78 needs a second staff account; rows 63/66 need a minted student token.** **Roadmap 4b math entry BUILT + PUSHED 2026-09-08 evening — `docs/math-entry-design.md` is the record.** Note `8aae6fb` (decisions 1.1–6.1 all = the recommendation: Unicode symbols + LaTeX only for `\frac{}{}` `\sqrt{}` `^{}` `_{}`; 22 keys in Structure / Operators / Greek rows; per-item "Math keys" toggle open only when the stem carries `$`; pointer keeps focus in the field, keyboard stays on the pad, roving tabindex; every insertion POSTS at once because a programmatic value change never fires `change`; the `\mathrm{…}` preview unchanged). **Slice 1a `182cfb9` = SERVER, DEPLOYED rev 15** (`lib/scoring/auto.ts` `canonicalizeMath` before the fold — `\frac`→`A/B`, `\sqrt`→`√A`, symbol / Greek synonyms → Unicode, Unicode super/subscript digits, a lone `x` between digit groups → `×`, parentheses dropped only beside `/` or `√`; design-tool 1327 tests; no migration; live, so the teacher-side auto-score rows can pass once the client is rebuilt). **Slice 1b `ae86af8` = client** (`swift test` 514, `xcodebuild` green; the vendored katex.min.js now runs in a bare `JSContext` in tests — `renderToString` needs only a three-method `document` stub). **42 rows** in `client/MANUAL-CHECKS.md` "Math entry — the keypad on short-text items" NOT run (fixture: paged, `$` stems, PLAIN keys `1/2` `x^2` `H2O` `sqrt(2)` `3.2 x 10^5` `45°C` `π` `<=` + one prose item; rebuild first; Cmd-Z row needs a real session). Digit pad → roadmap §Pilot feedback; follow-ups (teacher `math_input` flag, editor key-field keys + teacher-side KaTeX, numeric equivalence, Greek case, "next slot", drawing toolbar roving tabindex) = roadmap row 4b-f; handwriting spike post-pilot (Vision `VNRecognizeTextRequest`, thresholds in the note). Pattern held: Opus agents per build slice (1a ∥ 1b, disjoint files, same checkout, no agent commits), Sonnet for the rows, diffs reviewed + checks re-run here before each commit. **Next:** rebuild the client + run the 42 math-entry rows and the 35 drawing-tools rows in one sitting, then the next client release carries 4b + 4c; small opens from batches 3/5 listed 2026-09-08 (row 67 knob + `APP_COMMIT` as one XS slice, retention period = James's decision, rows 63/66/71 in the same Chrome sitting).
- **Delete draft + archive BUILT 2026-09-09 evening, slices 0–4, one commit each (`505fd9e` note, `0b81b24` delete draft, `f24802f` archive server + **migration 0031**, `6e8a4bc` archive UI, `4bf97ef` rows) — `docs/archive-and-delete-design.md` is the record (D-1…D-4: delete refuses with attempts via 409 `has_attempts`; `archived_at` on `assessments` + `test_sessions`; archive allowed on a Published row as the second status-only PATCH, 409 `session_open` while a sitting is open, sitting creation 409 `archived`; Delete on the Settings tab AND the list row; Show archived (n) on the home via `?archived=1` and on the Test sessions tab).** Design-tool 1413 tests. **S-f-1 (`de7c154`) + `APP_COMMIT` (`bc2a1ce`: CDK build arg → image ENV → `/api/health` `{ ok, commit }`) built the same evening. PUSHED + DEPLOYED 2026-09-09 ~21:30 PT: LIVE at https://<origin> = origin/main `bc2a1ce`, task def rev 18, rollout COMPLETED, `/api/health` returns the deployed commit = HEAD, Aurora at 0031 via `migrate-aurora.sh` (32 rows).** **Rows 109–119 RUN 2026-09-10 morning (Chrome on the origin): 10 ✅, 113 blocked (needs a current roster day for an open session); findings A-1 (list actions cell overflows → Delete behind a horizontal scroll) + A-2 (Unarchive from the archived view lands on the empty archived page) — BUILT + DEPLOYED 2026-09-10 midday, re-checked ✅: LIVE at https://<origin> = origin/main `3e35d36`, task def rev 21, health stamp = HEAD, Aurora at 0031 (A-1 needed `w-full max-w-0` on the name cell — auto table layout ignores a cell's max-width; a working rule).** 120's 404 half needs a second staff account; 121 a student. Pattern: Sonnet 5 slices 1 + 3, Opus 5 slice 2, diffs reviewed + checks re-run here; the dashboard tree is rendered headless by `reporting-views.test.tsx`, so list-row client leaves reload the page instead of `useRouter` (same as `DeleteAttemptAndReturn`).
- **Roadmap row R — rubric upload + AI feedback — BUILT 2026-09-11, slices 0–7, one commit each (`d7008cb` note, `36a5ffe` extractor + route + **migration 0032** guardrail surface, `542a1fc` upload dialog, `6a1e884` single-point scoring + per-criterion tables, `dd0bbe9` library + **migration 0033** `rubrics`, `f3be13e` print-page feedback, `334c812` `ai_usage` log line, then rows 122–153) — `docs/rubric-upload-design.md` is the record (D-1…D-7, James 2026-09-11).** Design-tool 1532 tests, typecheck clean. **PUSHED + DEPLOYED 2026-09-11 ~10:55 PT: LIVE at https://<origin> = origin/main `bf56c1f`, task def rev 22, `/api/health` commit = HEAD, Aurora at 0033 via `migrate-aurora.sh` (34 journal rows).** **Rows 122–142 ✅ 2026-09-11** (Chrome on the origin; findings R-1 `style_guess` noise / R-2 id wording / R-3 lock, proposals only, in the checks file; fixture `Rubric upload hand-run 2026-09-11` Draft stays on the origin). R-1 BUILT + **DEPLOYED 2026-09-11 ~11:35 PT: LIVE = origin/main `d61d703`, task def rev 23, health stamp = HEAD, no migration (Aurora stays at 0033).** Rows 122–123 re-checked on rev 23: no banner ✅ (`15fba2f`). **Tall-rubric scroll fix DEPLOYED 2026-09-11 ~12:33 PT: LIVE = origin/main `0d630b8`, rev 24, health stamp = HEAD; row 154 ✅ with James's real rubric.** Memory consolidated 2026-09-11 to seven durable files (overview, project-state, working-rules, deploy-recipe, aws-infra-reference, IT timing, headless Chrome) — this file stays the ledger of record. **Sitting fixture prepared on the origin: `Rubric sitting hand-run 2026-09-11` (Published, single-point hybrid essay, `with_feedback` on).** Rows 143 (second staff account) and 144–153 open (Bedrock rows need `RUBRIC_EXTRACTOR_PROVIDER=bedrock` / `ESSAY_SCORER_PROVIDER=bedrock` on the origin — `ESSAY_SCORER_PROVIDER` was already bedrock on the task; `RUBRIC_EXTRACTOR_PROVIDER=bedrock` added to the task definition in `app-service.ts`, ships with the deploy; 144–152 need a sitting with `<demo-student-A>`). Pattern: Opus 5 for slices 1 / 3 / 4, Sonnet 5 for 2 / 5 / 6 / 7; slices 3 ∥ 4 then 5 ∥ 6 ran in parallel on disjoint files in one checkout, each committed by path; running DB test files concurrently on the shared test DB gives spurious failures (the upload dedup row) — run the full suite only when no agent is active. `bedrockGuardrail` logs no tokens (policy units only).
- **Roadmap row T — time limit enforced + unfinished attempts — BUILT 2026-09-11, slices 0–4, one commit each (`90731f0` note, `ba8e168` server + **migration 0034**, `21b70e9` client, `1a47ff5` teacher UI, rows) — `docs/time-limit-and-unfinished-attempts-design.md` is the record (D-1…D-4, James 2026-09-11: review in-progress + force hand-in; the client ENDS the session at zero without handing in; dismissable banner + 5/1-minute notices; server refuses answers after deadline + 30 s).** Design-tool 1572 / schema 123 / swift 587 tests, xcodebuild green. **PUSHED + DEPLOYED 2026-09-11 ~14:30 PT: LIVE at https://<origin> = origin/main `e384d1f`, task def rev 25, health stamp = HEAD, Aurora at 0034 via `migrate-aurora.sh` (35 journal rows).** Teacher rows 155–167 + 19 client rows NOT run (row 167's deploy half done); the client half ships in the next release (v1.3.0 with 4b + 4c). `hand-in` route is owner-only and relaxes `session_open` once the deadline passed; `teacher_hand_in` is not client-postable.
- **Client managed-preference configuration BUILT + RELEASED 2026-09-11 (client v1.3.0)** — field report: v1.2.0 on two district Macs showed an empty white card (no Sign in with Google). Cause: the client id came only from `--google-client-id` / `SECURE_TEST_GOOGLE_CLIENT_ID` and the server URL only from `SECURE_TEST_SERVER` with a `localhost:3000` fallback; a Finder / Jamf launch sets neither, and every hand-run had gone through `client/scripts/launch-client.ts`, which injects both (the hand-install row was never run). Decision (James): managed preferences. Built (Opus 5 agent, reviewed here; `swift test` 601, `xcodebuild` green): Core `ClientConfiguration` (per value: launch argument `--server` / `--google-client-id` > env > managed preference `ServerURL` / `GoogleClientID` in domain `net.psd401.securetest.client`; blank = unset; a non-http(s) URL logged as unusable; NO localhost fallback), `AppDelegate` logs `config: <value> from <source>` at launch, the entry card shows "This Mac isn't set up for Secure Test yet. Ask your teacher or IT for help." when unconfigured (the `--token` dev posture unchanged), `client/config-profile.example.mobileconfig` (placeholders), `client/RELEASING.md` "Configuration profile", `client/MANUAL-CHECKS.md` "Managed-preference configuration (2026-09-11)" rows (a)–(h) NOT RUN. **Where it landed:** the other terminal's add-all commits swallowed the staged slice into `97c234f` ("Deploy record: time-limit server half live") and the 1.3.0 bump into `c37921e` — both pushed, `main` protected, so those commit messages do not describe it; this line and the two docs are the record. **v1.3.0 PUBLISHED 2026-09-11 ~15:30 PT** (built from `c37921e`; psd-sign 0.5.0 — new Step 8b puts a "Requires configuration profile" block with placeholder values in the release notes; `/releases/latest` = 1.3.0, 302). Jamf needs BOTH the pkg policy and a `com.apple.ManagedClient.preferences` profile scoped to the same Macs (real values → ops repo, next IT batch). Working rule: never edit or stage in this checkout while another Claude terminal is active in it — use a worktree or wait for the explicit close.
- **First district-Mac sitting DONE 2026-09-14 — v1.3.0 through Jamf, a real student, real sessions; batch 2 COMPLETE.** IT's Jamf install (pkg policy + the managed-preferences profile) worked without incident: a Finder launch shows Sign in with Google, so the MDM `Forced` payload reaches the sandboxed app; no TCC prompt. Two sittings (James at the student Mac, Claude in Chrome on the teacher side): `2026 AP Seminar EOC B` — mid-test rows 155 / 156 / 157 / 161 / 77 / 113 ✅, hand-in, essay on the per-student page, section filter (row 70) ✅; **the session was closed and the attempt deleted so the pilot teachers see the assessment fresh** — and `Time limit hand-run 2026-09-11` — the session ended on its own at zero with the "Time is up." sheet; rows 159 / 160 / 164 ✅ through the route, 162 route ✅ / UI ❌; that attempt deleted too, sessions closed. Records: `client/MANUAL-CHECKS.md` ("Signed build" last row, "Managed-preference configuration" (b), "Time limit"), `docs/design-tool-manual-checks.md` rows 70 / 77 / 113 / 144 / 155–165, `docs/client-release-plan.md`, `client/RELEASING.md`. **Findings (proposals only, nothing built; roadmap §Progress 2026-09-14 lists them):** **T-1** client — the banner's × freezes the strip instead of hiding it (`.time-limit { display: flex }` beats the UA `[hidden]`; fix = `.time-limit[hidden] { display: none }` + a test → **v1.3.1**); **T-2** design-tool — Hand in stays disabled after the deadline while the sitting is open (the route relaxes `session_open`, the three controls do not; rows need `deadline_passed`); **T-3** — the 1-minute notice not observed after hiding the banner (unverified, stderr unreadable on a Finder launch); **R-4** — no UI path to a FIRST AI proposal on `ai` / `hybrid` essays (`score-ai` has no caller; row 144's button never existed — pilot-relevant); **C-8** — no Beside / Above toggle on `own_page` sets (James expected one; options in the multi-source note). **The one-day teacher-row script was LOST and REBUILT 2026-09-14** at `~/secure-test-hand-teacher-row.sh` (outside every repo; takes the student number as its argument, `SECTION=<ps_id>` override, `end_date = current_date + 1` so an evening sitting survives the UTC flip, the SG rule revoked on exit); the roster row expires at the 06:00 import. Gotchas: the student Mac's Wi-Fi dropped between the sittings — the sign-in sheet opened blank and vanished (the presenter's silent-failure path, `signin_failed` in `errors.log`), not an app bug; the classifier denied the archive PATCH and the results-page batch that carried it (split writes from reads); an essay posts only on leaving the field, so a mid-typing matrix reads "0 of 1 answered".
- **2026-09-14 sitting fixes BUILT 2026-09-14 evening, three commits on `main` after `60bc22c` — PUSHED 2026-09-14 evening (origin/main `31b8cdb` + this line); **deploy HELD** by James (more design-tool work lands before the next `cdk deploy`; R-4 + T-2 are design-tool-only and go live with it); **v1.3.1 NOT cut** (T-1 is client-only and reaches the fleet only through a release + Jamf):** `acf8eb4` **T-1** client — `.time-limit[hidden] { display: none; }` + a stylesheet test (`swift test` 602, `xcodebuild` green; the T-1 row in `client/MANUAL-CHECKS.md` marked built, re-run on **v1.3.1** — bump `MARKETING_VERSION`, psd-sign per `client/RELEASING.md`, `gh release create` by James); `8539c15` **R-4** design-tool — "Score with AI" on the needs-manual card for `ai` / `hybrid` entries with no proposal (`offersAiScoring` + `ScoreWithAiRow` in `ScoringQueue.tsx`, posts through the existing `rerunAi` → `rescore-ai` path; row 144 reworded, re-run on a Bedrock sitting); `56c9964` **T-2** design-tool — `deadline_passed` on `ResultsRow` + the monitor's `AttendanceRow` via the hand-in route's own `deadlineFor` / `isPastDeadline`, the three Hand in controls enable on `!sitting_open || deadline_passed` (Delete unchanged — its route has no relaxation; row 162's UI half re-runnable). Design-tool 1587 tests + typecheck clean; no migration, so the deploy is `cdk diff` → `cdk deploy` only (Aurora stays at 0034). Records: the two design notes' §Progress + `docs/roadmap-2026-09.md` §Progress 2026-09-14. Still open from that sitting: T-3 (1-minute notice unverified) and C-8 (Beside / Above on `own_page`, James's decision). Pattern: Sonnet 5 for T-1, Opus 5 for R-4 / T-2, diffs reviewed + both suites re-run here before each commit; the repo has no DOM harness, so a React click stays a hand-run row and component tests assert a pure predicate + static markup.
- **Pilot-teacher export request + scoring corpus BUILT + DEPLOYED 2026-09-14 evening — LIVE at https://<origin> = origin/main `4c8120a`, task def rev 28 (rev 26 = `018a37c` carried R-4, T-2, the packet page and **migration 0035** → Aurora at 0035 via `migrate-aurora.sh`, 36 journal rows; rev 27 = the W-1 fix; rev 28 = stem-excerpt LaTeX symbols; **rev 29 = `543f04b`**: teacher-side KaTeX for short-text answers [4b-f half, `6b76314`], the row-67 throw knob [`aa6baab`, row 67 ✅ on the origin], CDK CLI 2.1141.0 [`3bb4a32`], corpus slice 4 in-VPC runner [`oneoff-aurora.sh`, proven on Aurora: 6 essays, 0 teacher finals]). The orphaned pre-batch-3 log group was DELETED by James the same night. **STATE AT CLOSE-OUT 2026-09-14 night — start here:** LIVE = rev 29, Aurora 0035, origin/main = this ledger commit; client v1.3.0 on the fleet. **v1.3.1 PUBLISHED 2026-09-14 ~20:33 PT** (`48afe60`, tag on `main`, `/releases/latest` = 1.3.1): T-1 + **deployment target lowered 26.5 → 26.4** (IT asked; highest-gated API is `allowsAccessibilityLiveCaptions` at 26.1, no `#available` checks, the 26.4 build is the proof; NO real session on 26.4 yet — James has no such Mac, IT runs the first one before widening scope; `client/RELEASING.md` + `docs/client-release-plan.md` record it). Jamf picks it up via Installomator; the v1.3.0 profile is unchanged. Next, in order: (1) rows 144 + 175 on a Bedrock sitting and the corpus Bedrock run once a pilot teacher has scored essays (`oneoff-aurora.sh corpus --with-human-final --dry-run` first); (2) row 170 measured in a real PDF + 171 with a long essay; (4) James's decisions: event-table retention period, C-8, numeric equivalence; (5) rows needing a second staff account (78 / 120 / 143 / 179) and the client rows (real session). Known flake FIXED (`golden CSV`, `018a37c`), health stamp = HEAD, rollout COMPLETED. Rows 168–179 RUN in Chrome (7 ✅, 173 half, 170 CSS-only, 171 / 175 / 179 NOT RUN); finding W-1 (the key printed under `scores=none` / `ai`) fixed + verified live. The flake + unscorable-reason follow-ups are in `018a37c`.** Two notes: `docs/student-work-export-design.md` (roadmap row X; D-1 labels + key page, D-2 every item type) and `docs/scoring-corpus-design.md` (row Y; D-3 third `scores.status` `research`, D-4 loss on attempt delete accepted). Commits: `87243ae` notes + roadmap; `226d203` corpus slice 1 — **migration 0035** (`scoring_runs`, `scores.run_id` / `prompt_version` / `rubric_snapshot`, `research` status, applied to dev + test), `ESSAY_SCORER_PROMPT_VERSION` = 2026-09-14 with a prompt-hash drift test (bump the date AND the recorded hash when the prompt changes), reader sweep so research rows are invisible (approve → 404); `fc128dc` slice 2 — `runEssayScorer` extracted from `aiScoreResponse`, `lib/scoring/corpus.ts`, `scripts/score-corpus.ts` (`--label` required, `--with-human-final`, `--provider mock|bedrock`, `--model`, `--dry-run`; exit 1 on zero matches, `created_by = cli:<user>`) + `lib/reporting/runComparison.ts` + `scripts/compare-runs.ts` (`--run <label>` / `--all`, `--csv`); `2e37690` slice 3 mock half run on dev (two rubric-less essays → unscorable; Bedrock half OPEN — needs pilot essays with teacher finals reachable: slice 4's in-VPC task or a row copy); `1301abb` export slice 1 — `lib/reporting/workPacket.ts` + `/dashboard/[id]/results/work` (owner-only, `?section` required else a chooser, `items`, `questions`, `scores=none|teacher|ai|both`, `anon`; page per handed-in attempt, ☑/☐ on every choice, latest AI row whatever its status, `@page 1in`, anonymous key page last); `b141618` slice 2 — GET toolbar (hidden `questions=0` + last-value-wins parsing), "Print student work" on the results page, **rows 168–179 NOT RUN**. Tests: design-tool 1707, typecheck clean. Known flake: `test/results.test.ts` "golden CSV" fails intermittently on `main` (pre-existing). Pattern: Opus 5 / medium for corpus 1 + 2 and export 1, Sonnet 5 / medium for export 2; every diff reviewed + both checks re-run here before each commit. Next: deploy (rev 26 + migrate 0035) → rows 168–179 in Chrome; the corpus Bedrock run; slice 4 in-VPC runner when a real corpus exists.
- **2026-09-15 — client end-state audit + v1.3.2 BUILT (not released), C-8 both halves BUILT, AP Seminar fixed live.** Pilot feedback: sources must default beside the question. Live fix in Chrome the same morning: `2026 AP Seminar EOC B` unpublished → student layout `paged` + set layout `side_by_side` → republished (the shared copies are the two teachers' own rows — copy semantics — they change theirs or James runs SQL; James is sharing it as "… v2"). Audit (Explore agent, verified here): nothing but `showEntry()` ever removed content from the window; the dev **watchdog ended EVERY real session at 600 s wall-clock** (no activity reset, no fleet override) then offered "Stay here"; a failed `begin()` and the 5 s gate backstop built the test unlocked; **zero `#if DEBUG`** — `SECURE_TEST_SIMULATE_LOCKDOWN=1` from Terminal ran a real test unlocked on the notarized build, no-entitlement fell back to a fake session, `--server` outranked the Jamf ServerURL, Cmd-O / `--token` / debug-crash reachable. Built, one commit each, Opus agents + diffs reviewed + suites re-run here: `f5c53b3` C-8(b) editor default (`lib/itemSetLayout.ts`, design-tool 1735); `5aabd3b` C-8(a) Beside / Above on own_page question pages when WIDE, Above = old presentation; `ebe20ab` **security slice 1** — `PageLoadGate.Outcome` (opened / refused / timedOut, 20 s backstop, only `.opened` builds), gate opens on `.active` only, every non-hand-in end → flush focused field + dirty drawings (`__secureTestFlushInput`) → `showEntry()` → one-button sheet ("Time is up." / "Secure session ended … rejoin from Your tests"), "Stay here" gone, `controller.retire()`; `49ac102` **security slice 2** — `BuildPosture` in the app target (Core stays config-agnostic): watchdog `TimeInterval?` nil in Release (James: disable), simulate / no-fullscreen / debug-crash / `--token` / `--bundle` + File menu Debug-only, Release without entitlement → `RefusedLockdownSession`, `ClientConfiguration(managedPreferenceWins:)` true in Release. swift test 626, Debug + Release xcodebuild green; `MARKETING_VERSION` 1.3.2. **35 client rows NOT RUN** (`client/MANUAL-CHECKS.md` "Content only while locked", "Release hardening", "C-8 — Beside / Above on own_page"), row 180 teacher. **PUSHED + DEPLOYED 2026-09-15 ~10:52 PT: LIVE at https://<origin> = origin/main `c7d98d3`, task def rev 30, rollout COMPLETED, health stamp = HEAD, no migration (Aurora stays at 0035).** Two hung-begin recovery rows added (Cmd-Q after a refusal; relaunch then join). **v1.3.2 PUBLISHED 2026-09-15 ~11:25 PT** (built from `fa39945`, tag on `main`, `/releases/latest` = 1.3.2 → 302; psd-sign from the session, `gh release create` James; archive kept its Developer ID signature, six entitlements + profile intact, app + pkg notarized). **Next: the 37 client rows on the released v1.3.2 in a real session (fixture `Client rows hand-run 2026-09-08` or `Row S-f hand-run 2026-09-09`; one-day teacher-row script first); Jamf/Installomator moves the fleet off v1.3.1's 10-minute self-end; delete the leftover slice-3 worktree.** Working rules: `form_input` on a React select does NOT update state — set `.value` + dispatch `change` via `javascript_tool`; `git apply --3way` refuses a file with unstaged changes — split by `--include`.
- **2026-09-15 afternoon — while v1.3.2 rolls out (IT's AutoPkg recipe, a few runs a day, never over a running app; pilot sittings begin 2026-09-17).** Decisions (James): **D-11** event-table retention 90 days on the three error / guardrail tables, `feedback` kept, swept by the nightly roster-sync; **numeric equivalence** for short text ON by default with a per-item "Answer form matters" checkbox; **T-3** verify-only. Built, one commit each, diffs reviewed + suites re-run here: `7f5d94c` D-11 sweep (`lib/retention/sweep.ts`, best-effort in `syncHandler`, row 190); `c61b3a9` numeric equivalence (`parseNumericAnswer` in `lib/scoring/auto.ts`, `exact_form` on the teacher bundle + jsonb config, editor hint + checkbox; **table cells share the rule** — a `0.5` key takes `1/2`; rows 181–185; design-tool 1804 / schema 124); `f5dc381` client hygiene = the audit's Medium items as a **v1.3.3 candidate, release HELD** (24 h stale-spool purge at launch + `showEntry`, `nonPersistent()` assessment web view, `errors.log` 500-line / 512 KB cap; swift 635). Also `c872bea` **row 180 ✅** on the origin; `497f1c5` `docs/pilot-quick-start.md` (teacher + student page + "looks wrong but isn't"; sent to James for review); `d9a683f` the re-published pilot assessment confirmed side by side on the student Mac. Plus `c030b29` operator modes **`seed-essays`** (pilot essays for the demo students without the client — five calibrated samples, one transaction, section admission not checked) + **`roster-health`** (existing script as a mode), infra README "Seeding pilot essays". **PUSHED + DEPLOYED 2026-09-15 ~13:05 PT: LIVE at https://<origin> = origin/main `c030b29`, task def rev 31, rollout COMPLETED, health stamp = HEAD, no migration (Aurora 0035); the importer Lambda bundle carries the sweep (first run 06:00 2026-09-16).** **Then, the same afternoon:** `roster-health` ✅ both pilot teachers (three current AP Seminar sections each; sync succeeded 06:00); five essays seeded on v2 via a section sitting (`JMDSHN`; real students of James's non-pilot section — James: acceptable, delete after) → **finding R-5** (3 of 5 Bedrock scores 502: the model renumbers level ids per criterion; 2000-token cut) → fixed `b5cc954` (`reconcileLevelIds` by points, 4000 tokens, prose-wrapped JSON, stopReason in the log, prompt version 2026-09-15) → **DEPLOYED rev 32 ~13:25 PT, health stamp = HEAD** → all five proposals: high 24 / mid 16 / low 10 / brief 8 / offtopic 6 of 24 (rows 191–194, 144, 175 ✅). Then **O-1..O-3 `33cbc0a` DEPLOYED rev 33 ~13:55 PT** (the afternoon's alarms: browser-closed page streams were 500 `request_error` → now `request_aborted` WARN, no row; next/image EACCES on `.next/cache` → `images.unoptimized`; scorer `console.error` → `log.error("essay_score_failed")`). Seeded attempts deleted + `JMDSHN` closed (James). **Rows 181–185 ✅ 2026-09-15** (185 in Chrome: hint + `exact_form` export/import round trip; 181–184 by one sitting on a six-item fixture, matrix 1/1/1/0/0/1 exactly as predicted; fixture + attempt deleted). Quick-start gained "Three clocks" (`0fc7c61`) after James asked whether Close / session length / time limit each end a test — only the time limit does today. **Pilot teachers need Close (and expiry) to end every attempt BEFORE 2026-09-17 → roadmap row CS, `docs/close-session-ends-attempts-design.md` scoped 2026-09-15, **D-1…D-7 DECIDED by James**: Close / expiry end the SITTING not the attempt (no hand-in — multi-session work; attempt stays in_progress + resumable; writes refused, no grace; client returns home via the peek poll / 409; Hand in stays the teacher's). **v1.3.3 hold LIFTED — the client half ships in v1.3.3 TODAY (with the hygiene slice); no server-side lever ends a v1.3.2 session.** Three slices: server (deploy tonight / Wednesday am), client → psd-sign v1.3.3 → ask IT for AutoPkg runs Wed pm + Thu before first period, docs (quick-start "Three clocks" rewrite + 1.3.2 fallbacks). NEXT SESSION STARTS THERE.** Also open: v1.3.2 client rows in a real session once the student Mac updates (still 1.3.1 at 14:30 PT). IT distribution corrected the same day: custom AutoPkg recipe, not Installomator (GitHub 60/hr API limit) — psd-sign 0.5.1, memory `fleet-update-autopkg`.
- **Row CS BUILT 2026-09-15 afternoon, three slices + the release-checklist commit, ON `main` NOT PUSHED (`9c1d142` server + **migration 0036** applied to dev + test, `e051bb9` client = v1.3.3, `34d5b81` docs, `c0cc571` `client/expected-entitlements.txt` + archive-only RELEASING.md) — `docs/close-session-ends-attempts-design.md` §Progress is the record.** Close / expiry → 409 `sitting_closed` (no grace) on response PUT/DELETE, both upload halves, submit — sitting check BEFORE the deadline; peek poll carries `sitting: open|closed`; close route returns `in_progress`; event kind `sitting_closed` client-written; Close dialog names the count (Monitor always, Test sessions tab only with Attendance expanded). Client: poll field / write 409 → `sitting_closed` event → security-slice-1 return-home + "Your teacher ended the test session." sheet. Quick-start "Three clocks" rewritten. Tests design-tool 1855 / swift 645, both xcodebuilds green. Rows 195–200 + 11 client rows NOT RUN. **psd-sign is 0.6.0** (pre-flight clean tree / HEAD = origin/main / version newer than latest; `expected-entitlements.txt` check; profile expiry ≥ 90 d; `PSDBuildCommit` = HEAD; notarytool log; `gh release create` printed for James; latest-redirect asserts the new tag; RELEASING.md entry). **DONE 2026-09-15 evening: PUSHED; DEPLOYED rev 34, health stamp = HEAD, Aurora at 0036; the row CS sitting ✅ (Debug build, simulated lockdown — rows 195/196/198/199 + six client rows; finding M-1 `$`+digit stems render raw under C-2, roadmap); `LSApplicationCategoryType` added (`f2cc2e5`); v1.3.3 PUBLISHED ~16:35 PT** (tag → `f2cc2e5` via `--target <full sha>`; `/releases/latest` → v1.3.3, asset sha matches the Desktop pkg; psd-sign 0.6.0 end to end — gotcha: the Skill tool substitutes `$1`/`$2` in SKILL.md with the invocation args, so no positional awk in a skill). **2026-09-16: v1.3.3 reached the student device through Jamf / AutoPkg overnight (no ask needed); the REAL-session CS run ✅ (Close → `sitting_closed` 0.4 s → `lockdown_end` 3 s → sheet; 197 Hand in ✅; Cmd-Q ✅); its refused essay flush → D-3 AMENDED: 10 s write grace (`bc13613`, server only; poll + submit ungraced). M-1 BUILT (`dd8f32d`, option b: `$`+digit opens math only with a LaTeX command / `^` / `_` inside, all three renderers). DEPLOYED rev 35 ~13:05 PT (`8caadb7`), then **rev 36 ~13:20 PT: LIVE at https://<origin> = origin/main `9fa68a6` (CS-1: a resume counts as Monitor activity + the quick-start Idle line), health stamp = HEAD, Aurora stays at 0036.** Findings CS-1 (resumed student shows Idle until first write) + v1.3.4 post-pilot items (essay autosave while typing, deferred `sitting_closed` spool entry, M-1 client half) in the roadmap. The `(copy)` fixture's attempt is Handed in by teacher — fixture can be re-archived. Pilot sittings begin 2026-09-17 on v1.3.3 / rev 35.** Open questions closed: "Hand in everyone now" not built (post-pilot candidate).
- **2026-09-16 evening — v1.3.4 client BUILT (not released) + hand-in-all + duplicate DEPLOYED.** Client, four commits on `main` (`6cdcf42` autosave 5 s idle / 30 s ceiling on essay, short_text, table cells, inline outline — `docs/client-autosave-and-deferred-spool-design.md`; `550836e` deferred spool: 409 `sitting_closed` holds the row, `flushDeferred` before the bundle fetch, additive `deferred_at` column; `019da10` 13 rows + `MARKETING_VERSION` 1.3.4; `d4f9e13` keypad tap sets the autosave baseline, drawing toolbar roving tabindex (4b-f), T-3 verify row, quick-start bullets say autosave is v1.3.4+). swift 674, both xcodebuilds green; **16 client rows NOT RUN** (`client/MANUAL-CHECKS.md` "Text autosave + deferred spool (v1.3.4)"; two need a real AAC session); **release HELD until after Thu 2026-09-17 14:00 PT** (pilot day 1; later if the rows are unrun) — then psd-sign 0.6.0 + `gh release create` (James) → AutoPkg next-day delivery. Design-tool, four commits (`4b05479` + `1562ae4` **Hand in everyone now**: `lib/api/handInAttempt.ts` shared with the per-attempt route, `POST /api/test-sessions/[sessionId]/hand-in-all` per SITTING, owner-only 404, closed/expired → every in-progress attempt, open → only past-deadline ones else 409 `session_open`, idempotent; `HandInAllControl` beside Close on the Monitor header and on every non-archived Test sessions row, `canHandInAll` predicate; `1d4e353` + `a6bbfcc` **Duplicate assessment**: `lib/api/duplicateAssessment.ts` = export → import (share-accept's pair) + one update carrying description / time limit / both allow flags, `POST /api/assessments/[id]/duplicate` 201, allowed on Draft / Published / archived, no confirm, lands in the copy's editor; list-row + Settings-tab buttons). Design-tool 1907 tests, typecheck clean, no migration. **PUSHED + DEPLOYED 2026-09-16 ~20:55 PT: LIVE at https://<origin> = origin/main `1562ae4`, task def rev 37, rollout COMPLETED, health stamp = HEAD, Aurora stays at 0036.** **Rows 201–205 ✅ 2026-09-16** (Chrome on the origin; the copies deleted); finding D-1 (list-row error squeezed the name column) BUILT `f4ce7fd` — **DEPLOYED ~21:22 PT: LIVE = origin/main `f4ce7fd`, task def rev 38, health stamp = HEAD**, row 205 re-checked ✅; **rows 206–214 ✅ 2026-09-17 morning** (one demo student on the Debug client from `d4f9e13`, simulated lockdown, two sittings `TGJDM6` / `S7PBD6` on `Client rows hand-run 2026-09-08 (copy)` + `Time limit hand-run 2026-09-11`, both closed and the fixtures RE-ARCHIVED after; 212's two-student half not exercisable). Also ✅ there: v1.3.4 idle-autosave row, the row-CS sheet + "Time is up." sheet on the Debug build; the 30 s ceiling row is ⚠️ (a pause happened — re-run with strictly continuous typing). **Finding H-1 BUILT + DEPLOYED 2026-09-17 morning** (`2c8cfbc` design-tool: `submitted_earlier` attendance status from one extra query, badge "Handed in (earlier session)" + note, counts under the Handed in tile, header "N of M joined · K handed in · O already handed in" with joined / handed in this sitting's own; `39778810` client: `JoinOutcome` compares `test_session_id`, refusal copy "You already handed this test in during an earlier session…" — rides v1.3.4). **LIVE at https://<origin> = origin/main `39778810`, task def rev 39, health stamp = HEAD, Aurora stays at 0036; row 215 ✅ live on the older `HMR4KB` sitting.** Design-tool 1912 / swift 677 tests. **Remaining 14 v1.3.4 client rows DEFERRED to later 2026-09-17**: fresh sitting on the `(copy)` fixture (unarchive; James deletes the demo student's submitted attempt there first; teacher-row script if past the 06:00 import), the launcher line is in the session notes: `SECURE_TEST_SERVER=https://<origin> SECURE_TEST_SIMULATE_LOCKDOWN=1 SECURE_TEST_NO_FULLSCREEN=1 bun --env-file=design-tool/.env.local client/scripts/launch-client.ts <Debug .app>`. Readings recorded in the commit messages: duplicate's settings update is a second statement after the import's transaction; hand-in-all is 404 for a foreign owner where the per-attempt route is 403; expired-but-open hands everyone in.
- **2026-09-17 (pilot day 1) — Extend time + access model slices 1–3 LIVE; deploy script in use.** Morning: six unscoped roadmap rows U-1…U-6 + U-7 pass back / U-8 timer (Side column; **pilot-week rule: design-tool work proceeds, NO client release before 2026-09-18 14:00 PT**). A pilot class timed out on day 1 → **Extend time BUILT + DEPLOYED the same afternoon** (`889cf38` server + **migration 0037** `attempts.deadline_override_at` + `deadline_extended` event; `621e1d8` UI beside Hand in everyone / Hand in; `6433ade` docs; rev 40; the client counts down to whatever the bundle carries, so v1.3.3 needed nothing; row 220 ✅, 216–219/221 unrun). `design-tool/infra/scripts/deploy.sh` (`b304c31`) = the whole recipe, allowlisted by path in `.claude/settings.local.json` (James added the rule — the classifier calls it self-modification). **Access model** (`docs/access-model-design.md`, D-1…D-8 decided, D-4 = roster-suggested co-teach grants, D-7 principals DEFERRED): `29ac206` slice 1 `lib/api/access.ts` authorize* replaces every inline owner check (403 → 404), `test/access-enforcement.test.ts` classifies every route; `13378b9` slice 2 **migration 0038** `access_grants` + `test_sessions.created_by_sub` + `assessments.owner_email` (backfilled from sittings), `ADMIN_EMAILS` via cdk context `adminEmails`, per-route levels view/run/edit/own, lists = owned ∪ granted, grants API; `365fb78` slice 3 Co-teach in the Share dialog (roster `role_name = Co-teacher` — the extract DOES carry it, 243 multi-teacher sections), rows 222–230 unrun. **2026-09-18: access slices 4 (substitute) + 5 (admin + impersonation) DEFERRED with 6 (James — substitute / admin / principal accounts revisited together; D-5 refinements recorded; a stopped agent's partial slice 4 is parked on `claude/access-slice-4-deferred`). Pass back (U-7) BUILT + DEPLOYED the same day — `docs/pass-back-design.md` (D-1…D-4): `c757221` server (**migration 0039** `superseded` score status + `attempts.pass_back_count` + `passed_back` event; `POST /api/attempts/[id]/pass-back` edit-level, deadline required when timed; the work packet's AI branch was the one reader not blind to superseded), `5173bf5` UI (Pass back on the per-student page + Monitor rows, Earlier scores section). LIVE = rev 42 (`5173bf5`), Aurora 0039, health stamp = HEAD, `ADMIN_EMAILS` = the maintainer (context `adminEmails` set 2026-09-18). Design-tool 2082 tests. Rows 231–238 (pass back) + 222–230 (co-teach) NOT RUN. `design-tool/infra/scripts/deploy.sh` has now run three deploys end to end.** **2026-09-21: James clarified two "admins" — the SYSTEM admin (maintainer, `ADMIN_EMAILS`) is wanted now, the BUILDING admin (principal) stays deferred → access slice 5a BUILT + DEPLOYED: `03fa3b6`, home toggle "All teachers" (`?all=1`, admin-only, Owner column, Open-now district-wide, row actions gated on `via === owner`); LIVE = rev 43, Aurora 0039, health stamp = HEAD; rows 239–242 ✅ 2026-09-21, 243–244 need a foreign sitting / second account; then A5-1..3 (Owner "you", all-view width → 6xl container) + `lib/db/isUniqueViolation.ts` cause-walk (a real sitting-code collision retried instead of 500) → **LIVE = rev 46 (`d4e59bc`), Aurora 0039, health stamp = HEAD; 2106 tests**. Impersonation + grants console (5b) stay deferred with 4 / 6.**
- **2026-09-21 evening — A5-4 + access slice 5 (impersonation) BUILT + DEPLOYED: LIVE at https://<origin> = origin/main `472372a`, task def rev 47, health stamp = HEAD, Aurora at 0040.** James's field report from the All-teachers view (every student "(unknown)", no section filter) → **A5-4** `d5fd870`: `buildResults(assessmentId, options)` + the review queue + the work packet scope students to the ASSESSMENT'S OWNER (new `assessmentOwner()` — `owner_sub` off the row, `owner_email` off the row else the newest sitting), which also fixed co-teachers on every results surface (only the Monitor was right). **Slice 5** `472372a` (D-8; decisions 2.1 target sub = owner_sub of the teacher's newest assessment / sitting, 404 `no_account_rows` otherwise; 2.2 admin keeps `own` everywhere outside an act-as): `POST /api/admin/impersonate` + `/stop`, `actor_sub` / `actor_email` on the JWT, **`isAdmin` is false while impersonating** (closes /admin, the toggle, chaining), **migration 0040** `impersonation_sessions`, "Acting as … · signed in as …" strip + Stop form in `AppHeader` (`AppChrome` shared by the dashboard + new `/admin` layouts), Act as beside Results on foreign all-view rows, `/admin` = open sittings district-wide with Monitor + Act as; grants console = 5b deferred. Design-tool 2131 tests. **Rows 245–252 NOT RUN** (`docs/design-tool-manual-checks.md`; 252 needs a second staff account). Record: `docs/access-model-design.md` §Progress.
- **v1.3.4 PUBLISHED 2026-09-21 ~17:45 PT** (built from `2168089cfe31`, tag on `main`; `/releases/latest` → v1.3.4, asset sha256 `a8179e3a…` matches the Desktop pkg; psd-sign 0.6.0 end to end from the session, `gh release create` by James). Carries autosave + deferred spool + H-1 client half + 4b-f toolbar tabindex + the hygiene slice + **the new app icon** (James's laptop-under-a-glass-dome on a Pacific tile, replacing the emblem tile; `sips`-resized into the ten `AppIcon.appiconset` slots). **The v1.3.4 sitting ran first the same afternoon: 15 of 17 rows ✅** (`client/MANUAL-CHECKS.md` "Text autosave + deferred spool"; two REAL AAC sessions on the phone's hotspot for the deferred-spool pair; T-3 CLOSED — the 1-minute notice seen on screen with the banner hidden; E12 row not exercisable; spool migration accepted on its unit test). Readings SI-1 / AS-1 / AS-2 in `docs/roadmap-2026-09.md` "2026-09-21" (proposals only; AS-1 = v1.3.5 candidate). Fixtures on the origin: `Client rows hand-run 2026-09-08 (copy)` (one in-progress attempt) and `Time limit hand-run 2026-09-11` (no attempt) — both unarchived, re-archive when done. Working rules: an up-to-date Debug `xcodebuild` re-runs only the stamp phase → Info.plist rewritten after codesign → SIGKILL at exec, `clean build` fixes it; `launch-client.ts` from the Bash tool needs the tool sandbox off; offline rows in a real session = phone hotspot + Auto-Join OFF on the other saved networks + Close from the phone. Fleet delivery via IT's AutoPkg recipe, expected 2026-09-22.
- **2026-09-22 morning — verification rows, Chrome half DONE; client sitting PREPARED and WAITING on the device.** Teacher rows 216 / 218 / 219 / 221 / 229 / 231 / 233 / 235 / 236 ✅ on the origin (rev 47), 222 half, six cosmetic readings E-1 / CT-1 / PB-1…PB-4 recorded as proposals in `docs/design-tool-manual-checks.md` (`b2cfa06` + the follow-up commit). Rows 223 / 228 / 230 HELD (a co-teach grant to a real colleague — James), 224–227 / 237 / 238 / 252 need a second staff account, 251's table half a pilot sitting open district-wide, 217 an in-progress student on an open sitting. **On the origin for the client sitting:** `Client rows hand-run 2026-09-08 (copy)` and `Row S-f hand-run 2026-09-09` unarchived, both attempts passed back with a Sep 23 23:59 override (232 / 234 ready); NEW fixture `Math entry + drawing tools hand-run 2026-09-22` (Published, paged, 13 items, from the gitignored `design-tool/samples/_build-math-drawing-fixture.ts`; M-1 means `$`-then-digit stems render raw — start the math with a letter). Sittings `8V5EBF` (copy) and `V8GCYB` (math) opened 2026-09-22, expire 16:01 PT; a later day = teacher-row script + new sittings. **The sitting runs on the district Mac once AutoPkg moves it from v1.3.3 to v1.3.4** (James's call; `docs/roadmap-2026-09.md` §Progress 2026-09-22 has the pass order). Gotchas: the pkg installs `/Applications/SecureTest.app` (no space); an off-fleet Release launch needs `SECURE_TEST_SERVER` + `SECURE_TEST_GOOGLE_CLIENT_ID` on the command line or a locally installed profile; this Mac cannot host a real session while Claude drives Chrome on it (10.7). Fixtures to re-archive after the sitting: the (copy), Row S-f, and the math fixture (delete).
- **2026-09-22 afternoon — practice sittings + co-teacher fix + small fixes DEPLOYED: LIVE at https://<origin> = origin/main `27a88c0` (the last two commits are another terminal's gradebook docs), task def rev 48, rollout COMPLETED, health stamp = HEAD, Aurora at 0041 (42 journal rows).** Commits: `f0328ef` E-1 / CT-1 / PB-1…PB-4 (R-2 / R-3 need no code) — **re-checked ✅ on rev 48** (rows 219 / 222 / 231 / 233 / 235; the Row S-f attempt was handed in + passed back again to reach the dialog, still until Sep 23 23:59, now "passed back 3 times"); `85667d7` **practice slice 1** (server, **migration 0041**: `test_sessions.kind` / `practice_for_sub`, `attempts.practice`, `students.practice_for_sub`; `requireStudentOrPractice` on all 12 student routes — a staff principal resolves ONLY through a practice sitting naming it; every class reader excludes practice, enforced by `test/practice-invisibility.test.ts`; Practice again relaxes `session_open`; nightly `sweepPracticeSittings` 7 days after close — D-1…D-8 APPROVED by James as recommended); `8146f72` **co-teacher tenant fix** (a LIVE bug found by reading: a join through a co-teacher's sitting filed the overlay + attempt under the co-teacher's sub while delivery / per-attempt routes / results resolve under the assessment owner → 404 on the bundle; `sittingTenantSub` = assessment owner on redeem + attempts + "Your tests"; read-only Aurora count the same afternoon: 0 sittings by a non-owner, 0 attempts filed under a co-teacher — nothing to repair); `b6bdded` the roster-sync Lambda gets `s3:DeleteObject*` on the asset bucket's `responses/*` ONLY (`ASSET_BUCKET` env, `infra/lambda/deleteStoredUpload.ts` — client-s3 only, refuses non-s3 / non-`responses/` keys) so the practice sweep deletes drawing bytes, first run 06:00 2026-09-23; `b7b29b4` delivery falls back to the co-teacher's overlay accommodations when the owner's row has NO live rows (any owner row, Off included, wins); `137d999` **practice slice 2** (teacher UI: "Practice on my Mac" = rest-of-day practice sitting, practice row with status line + See my answers + Practice again, Monitor's one row without Hand in everyone / Extend, Practice badges on Open now + `/admin`; review fix `sittingVisibleToCaller` — a practice sitting is listed only for its own teacher, `/admin` excepted). Design-tool 2176 tests. **Open:** practice rows 253–262 (need a teacher Mac with the client + profile — Jamf scope asked of IT 2026-09-22 — or an off-fleet launch); practice slice 3 (client copy: scope label, staff empty state, refusal copy) rides v1.3.5; slice 4 quick-start paragraph; co-teacher follow-ups not built (owner's Students page lists a co-taught student as "unlinked"; results section label / work-packet enrolment count use only the owner's sections); no uniqueness on open practice sittings per (assessment, teacher). Working rule: the query-aurora read works from the session; the classifier may deny a plain local `grep`/`sed` right after one ("Production Reads") — retry with the file tools.
- **2026-09-22 evening — v1.3.5 simulated hand-run DONE; two fixes in v1.3.5; NOT released.** Claude drove the Debug client via computer use (+ Chrome for the teacher side), James signed in: every v1.3.5 row that does not need a real AAC session ✅ and the security-slice-1 end rows re-run ✅ (`client/MANUAL-CHECKS.md`; roadmap §Progress "2026-09-22 evening"). Fixes: `5b26889` F-A (a refused / interrupted secure start stacked two sheets — `goHome` now returns unless the attempt screen is still up; F-B Escape-on-sheet ACCEPTED, comment corrected) and `19a4d8b` (failed sign-in logs `domain code: description` via `signInFailureSummary`, no authorize URL; swift test 693). origin/main = the docs commit after `db56551`; client only — nothing to deploy (LIVE stays rev 48). **Next for v1.3.5:** the three real-AAC rows (Close on the slowest Mac, every exit, time is up) + the v1.3.4 sitting on the district Mac, then psd-sign from `db56551` or later. Sittings `K5T6YB` / `M2NZPD` / `8857B9` closed; the math fixture's practice attempt `3b6da784…` stays (practice — invisible to results).
- **2026-09-23 — v1.3.4 district-Mac sitting DONE (real sessions; `docs/roadmap-2026-09.md` §Progress 2026-09-23 is the record).** Math entry 33 ✅, drawing tools 22 ✅, batch 0b 12 ✅, security slices 1 + 2 real-session rows ✅, rows 217 / 234 ✅, 232 in part. Findings: **EX-1 DECIDED** (the peek poll carries a changed deadline — v1.3.6 candidate, unbuilt), ME-1 (Shift-Tab skips the Math keys toggle), **ME-2 ACCEPTED** (no Cmd-Z in text fields — no Undo in the Edit menu; the canvas's own Cmd-Z works), ME-3 (VoiceOver reads literal LaTeX in the preview), HS-1 (hotspot regions unreachable by Tab). Contrast / zoom / font rows deferred. Working rules: VoiceOver on BEFORE lockdown; the teacher-row script EVERY sitting day (the row expired mid-morning the next day). Still open before v1.3.5: its three real-AAC rows. **Afternoon, same day — BUILT, pushed:** EX-1 server (`542842930822b6d2`, the peek poll carries the deadline + the Extend dialog's "shortens" hint) and **single-student print** (`d3331e6`: `/results/work?attempt=`, in-progress included and marked, per-student button + toolbar Student picker, anonymous single = the section packet's label) — **LIVE at https://<origin> = task def rev 50, health stamp = `d3331e6`, no migration (Aurora 0041)**; rows 268–270 ✅, 271–277 ✅ / in part (277 needs a practice attempt). **v1.3.5 now also carries** `ab7df2e` EX-1 client (a moved deadline restarts the countdown), `db26100` keyboard reach (HS-1 + ME-1 = one cause: with macOS Keyboard navigation OFF — the fleet default — WebKit's Tab skipped every button / radio / checkbox; now tabindex everywhere, radios one stop + arrows, one focus-ring rule) and `dc223c2` ME-3 (KaTeX TeX annotation stripped). swift test 716, design-tool 2198. **NEXT (a fresh session on ANOTHER machine, since a real AAC session breaks DNS on this Mac):** build v1.3.5 from origin/main, run `client/MANUAL-CHECKS.md` "v1.3.5 — EX-1 client, keyboard reach, ME-3" (14 rows, Keyboard navigation OFF, VoiceOver on BEFORE joining) + the three older v1.3.5 real-AAC rows, then psd-sign. Roadmap U-9 (batch print chosen students / sections) noted, not scoped. Gradebook: IT's second reply filed in the ops repo; slice 1 waits on seeing a snapshot carry the DCID columns.
- **2026-09-25 — guardrail fix LIVE + safeguarding alerts scoped. LIVE at https://<origin> = `8b2e266`, task def rev 55, rollout COMPLETED, health stamp = HEAD, Aurora at 0044 (45 journal rows, via `deploy.sh`).** Pilot report: an AP Lit teacher's "Score with AI" on one essay kept returning the raw code `blocked_by_guardrail`; `guardrail_events` (read via `query-aurora.sh`, categories only — never select `text_snippet`) showed essay-score / input / `content_filter` SEXUAL + VIOLENCE on every retry — the district guardrail (ADR 0012) cannot tell literary analysis from harmful content. Fix: `runGuarded` `inputMode: "record"` (input hit persisted as action `flag`, **migration 0044** widens the CHECK, the model still runs; output check still blocks), used only by `runEssayScorer`; the rescore-ai 422 carries a plain `detail` the queue shows. Design-tool 2241 tests. The teacher re-runs AI on that essay (expect `input/flag` then `output/allow`). **Row SG scoped, nothing built — `docs/safeguarding-alerts-design.md` (D-1…D-9):** alerts on first-person suicidal ideation / self-harm / abuse disclosures and prompt injection in handed-in essays + short text, to the teacher of the class in-app + email with the maintainer CC'd, prompt injection withholds the AI score behind "Score with AI anyway", alerts kept indefinitely, escalation outside the app; Bedrock Guardrails has NO self-harm category → own classifier (Haiku 4.5 vs Nova Micro / Lite measured in spike S-2 first, recall over precision), `PROMPT_ATTACK` in detect mode for injection (spike S-1); email waits on SES (HELD). **Environments decided:** no dev / prod branches; staging stack at the first deployed PowerSchool write (`docs/roadmap-2026-09.md` "Environments").
- **STATE AT CLOSE-OUT 2026-09-25 evening — start here in a fresh session. LIVE at https://<origin> = origin/main `5c55fc1`, task def rev 56, rollout COMPLETED, health stamp = HEAD, Aurora at 0045 (46 journal rows) — the deploy carried the held safeguarding work (slices 1–2, SG-F1, migration 0045) AND the two new public pages.** Built today (James): **`/roadmap.html`** (`design-tool/public/roadmap.html`, public, no sign-in: #journey = 23 dated milestones + 9 high points + a stats band, #ahead = the planned ORDER only, district-wide rollout after the pilot and before gradebook; a winding switchback road drawn in SVG from the markers; "Send feedback" / "Suggest a change" post to the staff-only `/api/feedback`, draft kept across sign-in) and **`/help.html`** (ten teacher topics + "Looks wrong but isn't", 9 screenshots + 2 GIFs in `public/help/`, click or Enter enlarges a picture; linked from the app header's **Help** and the sign-in page). Screenshots come ONLY from the local `secure_test_design_tool_demo` database — a fictional roster loaded through the real importer by `scripts/seed-demo.ts` (refuses any DB not named `*_demo`; prints a demo-teacher cookie) — and `docs/help-capture.md` + `design-tool/scripts/help-capture/` (sim-students.ts, make-pdf.ts, still.sh, anim.sh, content/) are the recapture recipe. **Routine upkeep (James): when a milestone ships, edit the JOURNEY / AHEAD arrays in `roadmap.html`; when a UI change touches a help topic, fix its steps and re-capture its picture.** Findings HC-1 (the Hybrid label promises a review that confident scores skip) + HC-2 (PDF proposals print raw type ids) are in `docs/roadmap-2026-09.md`, not built. Deploy gotcha learned: `next build` type-checks everything in the build context, so a dev script importing `design-tool/test` breaks the image — keep such scripts in `.dockerignore` (`5c55fc1`); and `deploy.sh` still runs `migrate-aurora.sh` after a FAILED `cdk deploy` (it then runs the old image's migrations — a no-op, but worth a guard with DS-1). Capture gotchas: Chrome must be in front at 100% zoom; typing through automation drops characters (set values + `input` event); Chrome's screenshot returns no file, so stills are one-frame recorder exports.
- **STATE AT CLOSE-OUT 2026-09-25 morning — start here in a fresh session.** **LIVE at https://<origin> = origin/main `bd9f989`, task def rev 54, rollout COMPLETED, health stamp = HEAD, Aurora at 0043 (no migration since rev 53).** **Client v1.3.5 PUBLISHED 2026-09-24 evening** (built from `0082272bd4fe`, tag on `main`; psd-sign 0.6.0 end to end, archive-only, six entitlements = `expected-entitlements.txt`, pkg sha256 `c0ba0e0d…31f5` = what `/releases/latest` serves; `gh release create` by James; `client/RELEASING.md` has the entry, `089726d`); AutoPkg moves the fleet on its own. Before the release, the "v1.3.5 — No time limit mid-test" client rows ran: 1–3 ✅ and 4 in part on a Debug build under simulated lockdown (Claude driving the client with computer use + the Monitor in Chrome, local dev), row 5 ✅ in a REAL AAC session on a Release build against the origin with the teacher side on a second machine; teacher rows 158 / 165 / 221 ✅ on local dev (`d5db328`, `0082272bd4fe`). **Narrow-window fix DEPLOYED (`bd9f989`, rev 54)** — a pilot teacher's half-width Monitor hid the row actions past the right edge (the table scrolled sideways only inside its own box; macOS hides that scrollbar and a vertical wheel never moves it): Monitor hides Progress / Last activity below lg and loses its nested scroll wrapper; the results matrix drops `min-w-max` and pins the Scoring column (Hand in) right; each Test sessions card's action group wraps (`shrink-0` → `min-w-0`); the attendance table hides Section / Last activity / Started below lg; the editor's tab strip wraps. Measured on local dev in a narrow same-origin iframe 480–1300 px (Chrome's `resize_window` did not change the viewport); CHECKED ON THE ORIGIN 2026-09-25 (rev 54, the Math entry fixture + the row-5 session, same iframe method in James's Chrome): Monitor row actions on screen 560–1300 px (off at 480, the known floor), matrix Hand in visible 480–1100 on 13 questions, Test sessions buttons never off-screen and all seven attendance columns fit at 1100 / 1300. Working rules learned 2026-09-24/25: a Release build ignores `--token`, so a real-session row needs Google sign-in and `SECURE_TEST_SERVER` set explicitly (the launcher falls back to localhost, and a client pointed at local dev lists local sittings — check Your tests before joining); a real AAC session breaks this Mac's DNS for Claude's own connection too, so the teacher side of a real-session row runs from a second machine; computer-use mouse clicks do not select radios in the test web view (keyboard Tab + Space does, and AXPress works on native buttons); two SecureTest instances share one bundle id and the computer-use tools see only one — quit the other first; the desktop app stops a `preview_start` dev server overnight and minted local sessions expire after 8 h.
- **2026-09-24 late — pilot-teacher time feedback BUILT + DEPLOYED: LIVE at https://<origin> = origin/main `89f989c`, task def rev 53, rollout COMPLETED, health stamp = HEAD, Aurora at 0043 (44 journal rows, via `deploy.sh`).** Three commits: `889f48f` Adjust time defaults to TODAY 23:59 (tomorrow only in the day's last minute) and every teacher-facing "Extend time" string is "Adjust time" (identifiers unchanged); `3daef55` **migration 0043** `attempts.time_limit_removed` + `test_sessions.time_limit_removed` — "No time limit" in the dialog, `deadlineFor` returns null for a removed attempt, both `/extend` routes take `ends_at` XOR `no_limit`, the sitting route takes optional `attempt_ids` (selected students; the sitting flag untouched) and otherwise sets / clears the sitting flag that later joiners inherit (`applySittingNoLimit` in `POST /api/attempts`), `attemptIsTimed` (a removed attempt is untimed for pass back), Monitor row checkboxes + select-all + "Adjust time for selected (N)" (not on practice sittings; the whole-session header control stays — it is the only path covering later joiners); `89f989c` the peek poll says `time_limit_removed: true` and **v1.3.5** stops the countdown, removes the strip and shows "Your teacher removed the time limit." (clients ≤ 1.3.4 count on until Resume). **Teacher rows 278–289 ✅ 2026-09-24 on LOCAL dev** (288 superseded by the client rows); **the five client rows "v1.3.5 — No time limit mid-test" are NOT RUN and gate the v1.3.5 release** (one needs a real AAC session). Design-tool 2239 / swift 721. Working rules: local dev had no migration 0042 (the cloud session applied it to Aurora only) — `bun db/migrate.ts` for dev + test; the built-in browser refuses localhost, so local hand-runs go through Claude in Chrome with a locally minted staff cookie (`mintSessionJWT`, dev secret) and minted student tokens; Chrome's screenshots are ~1.25× the page's layout width, so coordinate clicks miss — click by `find` ref; JS `.click()` bypasses a modal overlay and can open two dialogs at once — use real clicks for dialog rows; the hidden automation tab pauses the Monitor's polling — press Refresh.
- **STATE AT CLOSE-OUT 2026-09-24 evening — start here in a fresh session.** **LIVE at https://<origin> = `89fcf6e`** (two deploys today: `7b33384` gradebook push slice 1 + **migration 0042** `roster_dcids` → Aurora at 0042 / 43 journal rows; then `89fcf6e` roster-health DCID counts; health stamp = HEAD both times). Records: `docs/gradebook-push-design.md` §Progress, `docs/roadmap-2026-09.md` §Progress 2026-09-24. **Slice 1:** IT's extract columns (`students.dcid`, `sections.dcid` + `year_id`, `section_teachers.users_dcid`) are OPTIONAL columns in `lib/roster/extract.ts` (absent = keep the stored value, empty = NULL, non-integer refuses the row). **First import carrying the ids: 2026-09-25 06:00 → run `oneoff-aurora.sh roster-health`; the "DCIDs" block should read close to N / N** (all 0 / N with a succeeded run = the file came without the columns → ask IT). The Schoology consumer key / secret + `GRADEBOOK_TOKEN_KEY` are in `app-env` (hidden-prompt script, kept outside the repo; never overwrite `GRADEBOOK_TOKEN_KEY` — stored tokens would become undecryptable); nothing references them until slice 3. **Slice 3 (Schoology) waits on James's 8.3 / 8.4 / 8.5 + the Schoology app's callback URL (`https://<origin>/api/gradebook/schoology/callback`) being allowed; slice 2 (PowerSchool) on IT's test instance.** **v1.3.5: every row CLOSED 2026-09-24** (`client/MANUAL-CHECKS.md` — the 14 "EX-1 client, keyboard reach, ME-3" rows + the three older real-AAC rows, all ✅ on a Release build of `26f4824` from `~/Applications`, real sessions, Keyboard navigation OFF, VoiceOver for ME-3; reading EX-2 = a join on an already-expired attempt still calls `begin()`, v1.3.6 candidate). **Next: psd-sign v1.3.5 from origin/main (docs-only past `26f4824`) → `gh release create` (James) → AutoPkg.** Origin cleanup pending: close open sittings, re-archive `Client rows hand-run 2026-09-08 (copy)` + `Row S-f hand-run 2026-09-09`, delete the math fixture. Working rules learned 2026-09-24: `launch-client.ts` APPENDS the app's stderr to `~/securetest-student.log` (the terminal gets one line) — marker lines with `echo "--- x $(date +%H:%M)" >>` separate runs; the Monitor shows the SITTING's close time and each student's own deadline separately — read the row's note, not the header, before calling an EX-1 miss; VoiceOver reads page content with its own cursor (Control-Option-arrows) — Tab and mouse selection never reach static content (the page disables selection outside inputs by design); a cloud session has no AWS / Docker / `cdk.context.json` — deploys, `roster-health` and secret writes run on the Mac, and a fresh container needs `bun install`, a local Postgres and `bun run build` in `packages/schema` before the design-tool suite (2208 pass); gitleaks must be installed for the pre-commit hook or commits are refused.
- **Batch 2b slices 1 + 2 DONE 2026-09-04** (`docs/public-release-plan.md` §Progress is the record): HEAD is scrubbed — `cdk.context.json` untracked (six context keys incl. `domainName` + `guardrailId`, the stack throws without them; the real file is on this Mac only), the IT / warehouse request docs + the go-live checklist live in untracked `_ops-staging/` (on disk, destined for the ops repo — do not delete the folder), demo students are `<demo-student-A/B/C>`, `.gitleaks.toml` + the fixture guard test are in; the check is `gitleaks git --pre-commit --staged --config .gitleaks.toml`. **PUBLISHED 2026-09-04 evening: this repository is now the public `psd401/secure-test`** (root commit `3bd723c`; `origin` = public, `ops` = the private `psd401/secure-test-ops` with the full pre-release history — pushes go to `origin` only). `main` is protected (no force-push / deletion, linear history), secret scanning + push protection, Dependabot, private vulnerability reporting are on; the `scan` workflow passed on the root commit. The IT request for batch 2 slice 2 (the Developer ID profile) was sent 2026-09-04. Batch 2b COMPLETE: the ops repo is slimmed (`~/code/secure-test-ops`, two commits, pushed) and the local staging folder is gone. Next: batch 3 observability (`docs/observability-design.md`; slice 1 Sonnet, 2 + 4 Opus in parallel worktrees, 3 Sonnet).
- **Next session — do first (superseded above, kept for the record):** (1) Batch 2b slice 1 (`docs/public-release-plan.md`) — the public-repository sweep; slice 2 of batch 2 when the profile exists. Batch 1 rows are DONE; the `E6 / E7(b)` fixture now carries a submitted attempt and stays until its client rows run. (2) When a student sitting is next scheduled: the batch 0b rows, the E6 / E7(b) / E12 client rows (`client/MANUAL-CHECKS.md`), then delete the E6 / E7(b) fixture and the client-fixes fixture on the origin; row 58 on local dev if the JSON evidence is wanted. (3) Then batch 2 slices 1–3 (`docs/client-release-plan.md`) ∥ batch 2b slice 1 (`docs/public-release-plan.md`). The CDK CLI bump stays "after a deploy, never folded into one"; E11 waits for teachers. The Sep 2 roster morning check is DONE (5e5968a).
- **Open decisions (James's):** E11 rescoring after a key change — wants teacher input first, do NOT build; E3 table item — DECIDED + BUILT + hand-run 2026-09-02/03, one follow-up decided and unbuilt (E3-F1 above); client paging — BUILT + hand-run, P-1 decided and unbuilt; E12 per-student stimulus — design page written 2026-09-02, D-1…D-6 DECIDED the same day (any saved text; warn; same owner v1, cross-owner post-MVP; student writes the outline inline when missing — mechanism to confirm; render normally; results indicator); slices 1–4 BUILT 2026-09-02 (unpushed), slice 5 = hand-run rows 46–47 + client rows; per-question paging in the client (`docs/plan.md` Phase 2 — `own_page` renders inline until then); SittingsPanel hydration stall CLOSED 2026-09-02 — hydration is deferred while the document is hidden (background tab / covered window), the panel's first fetch waits for the paint; James confirmed the window was covered when it stalled, and uncovered the page is ready in ~2 s (`docs/ux-pass-2-proposal.md`); not an app bug, Securly not implicated; row 19 colleague run pre-pilot; row 29 share-and-accept (needs a second staff account); a post-flip student run against the origin. DONE 2026-09-02 and recorded in `docs/pdf-import-enhancements.md`: E6, E7(b) (plain-or-folded rule), E9 slices 1–2 (per-student form assignment DEFERRED post-MVP), E13, E14. Proposals not done: CDK CLI 2.1124.1 pinned vs 2.1139.0 (`bun update aws-cdk` in `design-tool/infra/`). **Public release (decided 2026-09-02): publish a FRESH repository from a squashed, swept clone — never flip this one public.** Its history carries the AWS account id (12 files today), the cluster secret ARN and the district NAT address (infra README), the Google client id, staff and student-domain emails (27 / 29 files), the production hostname, docs quoting teacher-authored assessment content (4 files), the ClassLink era, and one real student number in two 2026-08-29 commits plus one commit message (scrubbed from HEAD in 1dc4383; a history rewrite here is deferred into that release sweep unless district policy forces it sooner). The private repo stays the full internal record.
- **Working rules learned 2026-09-02:** `bunx drizzle-kit generate` needs `DATABASE_URL` set even though it is offline (any value; the config asserts it) — then apply with `bun --env-file=.env.local db/migrate.ts` for dev and `DATABASE_URL=…_test bun db/migrate.ts` for test; a table that references a table that references it back needs `(): AnyPgColumn => …` on the new reference or every row type silently becomes `any` (shows up as phantom errors elsewhere); the auto-mode classifier blocks security-group changes, Secrets Manager reads, production DB writes and browser actions on pages naming students — write the exact script to the scratchpad with the Write tool and hand it to James (`! bash <path>` runs it as one shell); automation-driven Chrome tabs are hidden, so a page's client code does not hydrate until something paints it — take a screenshot before reading state; `git grep -E '<ids>'` before committing any doc that mentions a student; Bash `cd` persists between calls — use absolute paths.
- **Working rules learned 2026-09-01:** deploy = `cd design-tool/infra && bunx cdk deploy` (no flags; run `bunx cdk diff` first; in auto mode the classifier blocks it — propose, then run when James says so); a deploy that carries new migrations is followed by `scripts/migrate-aurora.sh`, **deploy first** (the image carries its own migrations) — SUPERSEDED 2026-09-25 by DS-1: the container migrates at boot, `deploy.sh` has no migrate step, and DS-2 refuses migration-carrying deploys on weekdays 07:00–15:30 PT unless `--during-school`; `packages/schema` is consumed through `dist` — `bun run build` there after editing `src`; `cd client && bun scripts/vendor-katex.mjs` after a katex bump or a `macros.ts` edit (the schema package's macros test drift-checks it); `design-tool/samples/_run-import.ts` (gitignored, beside the teacher PDFs) mirrors the import route for Bedrock evidence — `PDF_EXTRACTOR_PROVIDER=bedrock bun --env-file=.env.local samples/_run-import.ts <name-filter>`; if `bun run dev` ever shows more than a handful of `postcss.js` processes, stop and `bun run dev:clean` (Turbopack cache → six kernel panics 2026-09-01); the district's egress address is the WSIPC/K-20 NAT shared by the whole network — never an SG rule; hand-built PDFs for tests live in `design-tool/test/helpers/pdf.ts` (no teacher PDF enters the repo).

## Contributing / what stays out of this repo

This repository is public. `docs/public-release-plan.md` is the record of
the sweep that made it so; the rules below are what keeps it that way
going forward (its slice 5).

- **No account ids, ARNs, hostnames, client ids, emails or personal names
  in tracked files.** Use roles ("the data engineer", "the IT director",
  "a teacher") and placeholders (`<account-id>`, `<origin>`,
  `<your-sso-profile>`, `<TEAM_ID>`) instead. The maintainer's name is the
  one exception — it stays as author/maintainer in ADRs and this file's
  README-equivalent framing; his email and AWS SSO profile name are
  scrubbed like everyone else's.
- **Student data never.** No real student name, number, SSID or email
  goes into a tracked file, commit message, or test fixture — not even
  temporarily. The three demo accounts used in hand-runs appear only as
  `<demo-student-A>` / `-B` / `-C` placeholders (their values live in the
  ops repository) — no student-shaped identifier is tracked. `git grep` for
  the patterns below before every commit that touches a student-facing
  doc or fixture.
- **Teacher-authored content never.** No verbatim quote, table, or
  figure description from a real teacher's assessment goes into a doc —
  design pages and hand-run records use hand-built fixtures (see
  `design-tool/test/helpers/pdf.ts`) or paraphrase instead. A finding
  about the importer's behavior on real content can still be recorded;
  the content itself can't be quoted.
- **District infrastructure specifics go to the ops repository**, not
  here: IT/vendor request drafts that name people and dates, real
  staff/section data, day-of hand-run scripts. That repo is private and
  referenced here only as "(internal — see the ops repository)", never
  by path.
- **Infra config lives in context, not source.** `design-tool/infra/`'s
  account id, OAuth client ids, MWAA role ARN, Bedrock guardrail id and public `domainName` are
  read from `cdk.context.json` (gitignored) via CDK context, never
  hardcoded — see `design-tool/infra/cdk.context.json.example` and that
  directory's README. `DesignToolStack` throws a clear error at
  synth/deploy time if `domainName` is missing rather than falling back
  to a real hostname.
- **The pre-commit scanner blocks what it can; review catches the
  rest.** `.gitleaks.toml` at the repo root extends the default gitleaks
  ruleset with rules for student-domain addresses, SSID-shaped numbers
  near roster field names, digit runs in markdown, AWS account ids near
  AWS-shaped context, and Google OAuth client ids. Run it on the staged
  changes before committing anything that might carry one of the patterns
  above: `gitleaks git --pre-commit --staged --config .gitleaks.toml`
  (a directory scan, `gitleaks dir . --config .gitleaks.toml`, also reads
  gitignored files such as `.env.local` and build output, so expect hits
  there that never reach a commit). It is
  not a substitute for review — a hostname or a person's name is
  identifier-shaped, not credential-shaped, so gitleaks' default rules
  (and most of the added ones) cannot catch a new one on their own; the
  patterns in this section are what a human reviewer checks for.

